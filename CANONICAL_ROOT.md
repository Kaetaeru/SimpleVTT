# SimpleVTT branch routing — `main` landing copy

This file exists on `main` so the GitHub default-branch view cannot be mistaken for current V1 development authority.

```yaml
main_role: historical/default-UI landing reference
product_integration_target: work/v1-composite
active_convergence_parent: agent/resolver-foundation-convergence
```

## Rules

1. Do not start or merge current V1 product work into `main` by default.
2. Read the live routing/status from [`agent/resolver-foundation-convergence/docs/CURRENT.md`](https://github.com/Kaetaeru/SimpleVTT/blob/agent/resolver-foundation-convergence/docs/CURRENT.md) while the current Resolver convergence sequence is active.
3. `work/v1-composite` remains the V1 product integration target.
4. Old Phase/handoff files on historical branches are evidence only and must not override the current status/architecture documents.
5. Changing the GitHub default branch or promoting another integration model requires an explicit owner decision; this pointer commit does not perform such a promotion.
