# W5-07 Host / P1 / P2 Action Matrix Evidence

Gate: `W5-07`  
Classification: `REUSE_LOCKED`  
Scope: `H/P1/P2 attack · check · save · spell · item · feature action matrix`  
Scenario families: `MP-03`, `MP-C01~MP-C24`  
Exact verification SHA: `0a9aeb6417c83df5f5d601b3112744cb6bd5e39a`  
W5-07 AUTO run: `33691718281` / job `100451682886`

## Verdict

The current production connected action path satisfies the W5-07 automated action matrix without a product-code repair. GitHub Actions `W5-07 AUTO Verification` completed successfully at exact SHA `0a9aeb6417c83df5f5d601b3112744cb6bd5e39a`; the focused suite reports `18/18 PASS`, `0 FAIL`, and its named core regressions collectively cover every `MP-C01` through `MP-C24` scenario.

This record is ready for official ledger/current reconciliation. Until that later reconciliation commit lands, the authoritative `V1_EVIDENCE_LEDGER.json` remains unchanged for W5-07.

## Exact-SHA automated evidence

- Workflow: `.github/workflows/w5-07-auto.yml`
- Run: `33691718281`
- Job: `100451682886`
- Exact SHA: `0a9aeb6417c83df5f5d601b3112744cb6bd5e39a`
- Result: `success`
- Focused tests: `18 passed / 0 failed`
- Artifact: `W5-07-AUTO-0a9aeb6417c83df5f5d601b3112744cb6bd5e39a`
- Artifact ID: `9870276424`
- Artifact digest: `sha256:b460f28a21ab72f18845891207f20681b39f9e65ef4bb2790d6753112ad26555`

The passing subtests map the full core matrix:

- `MP-C01/C03` remote player attack ownership and public fan-out;
- `MP-C02` Host NPC attack;
- `MP-C04` miss semantics;
- `MP-C05` critical semantics;
- `MP-C06` advantage selected/discarded dice;
- `MP-C07` multi-die damage;
- `MP-C08/C11` remote no-target ability check and public die presentation;
- `MP-C09` remote saving throw;
- `MP-C10` Influence/Search/Study picker-selected skill intent;
- `MP-C12/C15` remote attack spell/cantrip presentation;
- `MP-C13` saving-throw spell target/result association and public DC;
- `MP-C14/C16` slotted multi-target spell and slot debit;
- `MP-C17/C19` remote feature/resource use;
- `MP-C18` remote consumable item use;
- `MP-C20` disabled action rejection;
- `MP-C21/C23` invalid-target rejection and duplicate request suppression;
- `MP-C22` concurrent intent ordering;
- `MP-C24` duplicate terminal event-batch suppression.

## MP-C10 picker proof

`src/app/productionPlayRuntimeAdapter.ts` already projects picker choices as distinct authoritative actions rather than as a separate network-only skill field:

- Influence uses IDs such as `action.standard.influence.persuasion`;
- Search uses IDs such as `action.standard.search.perception`;
- Study uses IDs such as `action.standard.study.investigation`.

`src/app/connectedActionRoutingAdapter.ts` receives the remote `action-request`, validates the controlled actor and exact requested `actionId`, resolves the corresponding Host-authoritative action, and publishes the resulting connected resolution presentation.

`tests/ui/connectedThreePeerRemoteStandardCheckPicker.test.ts` proves one representative selection from each picker group. For each case it establishes Host + P1 + P2, selects the exact projected skill action, sends that exact `actionId` from P1, supplies the normal Host DM ability-check DC adjudication, completes the Host resolution, and requires both allowed remote consumers to receive the same public Host result with the same selected action identity.

## REUSE_LOCKED decision

The first focused run after adding MP-C10 coverage (`33691596880`) stopped the new check at the expected ability-check DM-adjudication boundary because the new test helper had omitted the DC step. The existing canonical MP-C08/C11 regression already handles that boundary by applying `ability-check-dc` before continuing. Reusing the same harness behavior fixed the regression; no production source change was needed.

Therefore:

- Reproduced product failure: **none**.
- Missing production action entrypoint: **none**.
- Need for a new network picker/skill field: **none**.
- Product-code modification authorized by `REUSE_LOCKED`: **no**.
- Product changes in this focused repair: **none**.
- Test/workflow evidence changes: `tests/ui/connectedThreePeerRemoteStandardCheckPicker.test.ts` and `.github/workflows/w5-07-auto.yml`.

## Next bounded action

Reconcile W5-07 as `PASS` in `docs/roadmap/V1_EVIDENCE_LEDGER.json`, `docs/roadmap/EVIDENCE_CARD.md`, `docs/roadmap/CURRENT.md`, and `docs/CURRENT.md`, then advance the canonical next Gate to W5-08.
