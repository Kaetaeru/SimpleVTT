# W5-01 Connected Topology and Participant Lifecycle Evidence

Gate: `W5-01`  
Classification: `REUSE_LOCKED`  
Scope: `H/P1/P2 topology and P3 late-join/reconnect lifecycle` (`MP-A`)  
Evidence product SHA: `2ac28651312f1fdbe82edb74fd13f342a8f910f7`  
Canonical evidence-base SHA when this record was prepared: `8a4d279d0144abdabeb8c1402cf86c5f55defe64`

## Verdict

The existing production connected-session path satisfies the W5-01 lifecycle contract without a product-code repair. The exact verified product tree remains unchanged through the canonical evidence-base SHA; only roadmap/current evidence documents changed between the two SHAs.

This focused record is ready for official ledger/current reconciliation. Until that reconciliation commit lands, the authoritative ledger remains `PENDING` for W5-01.

## Automated connected lifecycle evidence

GitHub Actions UI run `33636100212`, job `100267245818`, completed successfully at exact product SHA `2ac28651312f1fdbe82edb74fd13f342a8f910f7`.

Relevant successful steps include:

- `Verify v1 shell, connected topology, continuity, and Session layer contracts`
- `Verify Phase 14 live lifecycle, late join, connection safety, DM flows, local projection ownership, fresh Character skills/actions, inventory durability, spellcasting, and turn-event transport`
- `Typecheck and build`

Focused production regressions on the same tree establish the participant lifecycle semantics required by MP-A:

- `tests/ui/productionParticipantLifecycle.test.ts`
  - rejects incompatible protocol hello without participant/peer/ledger/projection ghost state;
  - rejects invalid SessionProjection before any ghost state is created;
  - accepts a genuinely new participant into a live Session and returns the current Host-authoritative ordered history;
  - rebinds a previously accepted participant to a replacement peer and returns ordered catch-up without replacing Host runtime authority.
- `tests/ui/productionClientReconnect.test.ts`
  - reconnects from the last accepted replica cursor;
  - applies the Host hello-ack catch-up exactly once;
  - resumes at the authoritative revision/cursor before further actions;
  - replayed catch-up does not duplicate mutation or participant state.
- Phase 12 connected CI additionally exercises the production connected Session lifecycle, participant lifecycle, persisted Character ownership, client reconnect, hello replay, Session end, wire/protocol, and shared runtime paths.

Together these cover the MP-A lifecycle core: valid/invalid join, live late join, disconnect/replacement reconnect, ordered catch-up, compatibility rejection, revision consistency, and exactly-once replay behavior.

## Windows topology evidence inherited on the same product SHA

V1 Tauri Verification run `33636100197` completed successfully at the same exact product SHA.

Job `100267245684` (`tauri-w4-07-spatial`) launched the existing production Tauri connected topology and passed both required Windows steps:

1. `Verify W4-07 G01/G02/G03/G08/G09 in Windows Tauri H+P1+P2`
2. `Verify W4-07 G04/G05/G06/G07 in Windows Tauri H+P1+P2`

The second path uses the existing handout runner's replacement-player reconnect lifecycle; the runner launches Host, P1, and P2 desktop instances and replaces the disconnected Player with a reconnect instance before validating authoritative shared state.

Artifact:

- ID: `9849024415`
- Name: `SimpleVTT-W4-07-G01-G09-2ac28651312f1fdbe82edb74fd13f342a8f910f7`
- SHA-256: `b044f73320cd10bf2446695677ca2098c7c162d01190feed7ecc599078c656a0`

This is reused topology evidence, not a new W5 product implementation. The master roadmap reserves final full rendered multi-window parity, P3 late-join, disconnect/reconnect, privacy and release-artifact acceptance for W9; W5-01 fixes the already-implemented connected topology/lifecycle contract and must not duplicate that final release harness.

## Current-tree inheritance check

Git compare from product SHA `2ac28651312f1fdbe82edb74fd13f342a8f910f7` to canonical evidence-base SHA `8a4d279d0144abdabeb8c1402cf86c5f55defe64` is ahead-only and changes only:

- `docs/CURRENT.md`
- `docs/roadmap/CURRENT.md`
- `docs/roadmap/EVIDENCE_CARD.md`
- `docs/roadmap/V1_EVIDENCE_LEDGER.json`
- `docs/roadmap/evidence/W4-07.md`

No production source, connected-session transport/runtime, tests, Tauri runner, or workflow implementation differs across that range.

## Reimplementation decision

- Reproduced current-tree product failure: **none**.
- Production reachability/contract gap requiring repair: **none identified for W5-01**.
- Product-code change permitted by `REUSE_LOCKED`: **no**.
- Next action: reconcile W5-01 as `PASS` in the official evidence ledger/current documents, then advance to W5-02.