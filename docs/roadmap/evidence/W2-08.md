# W2-08 Evidence Card

Gate ID: `W2-08`

Classification: `VERIFY_ONLY` → current-HEAD `REPAIR`

Acceptance criterion: In real Windows Tauri, create martial, prepared-caster, spontaneous-caster, pact-caster, shapeshifter, and healer Characters through production UI; persist them; terminate the app; relaunch with the same data root; and verify durable identity/content plus rendered Full Sheet.

Production entrypoint: Character → 새 캐릭터 → Guided Create → 모험 시작 → Character Library / Full Sheet → process restart.

Existing implementation files:
- `src/app/characterCreationV10Adapter.ts`
- `src/app/characterLibraryRuntimeAdapter.ts`
- `src/app/characterSheetV10Projection.ts`
- `scripts/run-tauri-e2e.mjs`

Existing automated tests:
- `tests/ui/characterCreationCompleteGate.test.ts`
- `tests/ui/productionFreshCharacterSpells.test.ts`
- Existing Windows Tauri/WebDriver harness

Existing Tauri/Windows evidence: See exact evidence below.

Exact observed failure: At SHA `e2bc905e545080664b636c0ad17328cf4ca1f641`, run `33461161848`, job `99711359200` reached real Character creation and failed with `prepared caster: no spell-slot capacity reached the durable sheet`. Guided Create persisted acquired spells but omitted initial class tracks and standard/pact spell-capacity source facts.

Smallest required change: Reuse `multiclassSpellSlots` and `pactMagicProgression` in the existing Character materializer, add the class-breadth regression, and let the existing W2 harness recognize the separate pact-slot field. No new store, runtime, persistence layer, or E2E framework.

## Exact evidence

- Product verification SHA: `699c923dc50dac5ecd266ced58568d3b647850b6`
- Product tree: `449bf34d36c7975addcc2196b87a9a026d525730`
- Branch: `agent/v1-w2-tauri-archetype-verification`
- Pull request: `#198`
- Command: `npm run test:e2e:tauri -- --w2`
- GitHub Actions: run `33470259003`, job `99738402317` — success
- Windows artifact: `SimpleVTT-W2-Tauri-699c923dc50dac5ecd266ced58568d3b647850b6` (ID `9786387605`)
- Artifact digest: `sha256:3077663cb94c2a444046d94808c76a677c7d92de61a0576f61d5f57b8cd1172d`
- Related exact-SHA checks: Contract validation, UI, Legacy Execution Boundary, Phase 11 Playable, and Phase 12 Connected Session — success.

Artifact files include `w2-08-{martial,prepared-caster,spontaneous-caster,pact-caster,shapeshifter,healer}-{created,restarted}.{png,txt}` and `w2-08.json`. Every Character was created through visible production controls, persisted, loaded after a real process restart with the same data root, and verified against rendered HP/AC/features/spells/actions/resources as applicable.
