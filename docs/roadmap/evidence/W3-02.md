# W3-02 Evidence Card

Gate ID: `W3-02`

Classification: `REUSE_LOCKED`

Acceptance criterion: initiative, round, turn, and action economy follow one consistent production lifecycle.

Production entrypoint: `ProductionPlayScreen` exposes the real start-initiative, end-turn, and end-initiative commands and renders current round/actor state. Those commands use the existing Phase 09 turn runtime adapter/service, which orders initiative through the domain initiative authority and advances end-turn/begin-turn through the generic Resolver while projecting one authoritative economy state back to the scene.

Existing implementation files:
- `src/ProductionPlayScreen.tsx`
- `src/app/phase09RealTurnRuntimeAdapter.ts`
- `src/app/realTurnRuntimeService.ts`
- `src/domain/initiative.ts`
- `src/domain/turnEconomy.ts`
- `src/domain/resolution.ts`

Existing automated tests:
- `tests/ui/phase09RealTurnRuntimeService.test.ts`
- `tests/ui/phase09RealTurnRuntimeAdapter.test.ts`

Existing Tauri/Windows evidence: Not required for this `REUSE_LOCKED` Gate. The complete rendered local-session lifecycle is owned by `W3-08`.

Exact observed failure: None. No product-code change is authorized.

Smallest required change: Reuse the existing production lifecycle and record exact-SHA automated evidence only.

## Exact evidence

- Verification SHA: `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`
- GitHub Actions UI run/job: `33498144567` / `99824979798` — success
- Gate-owned focused tests: 12 total (5 service + 7 adapter).
- `phase09RealTurnRuntimeService.test.ts` proves initiative ordering, round-1 startup, begin-turn Action/Bonus Action/Reaction/movement initialization, committed economy synchronization, end-turn/begin-turn ResolutionEvents, round wrap, next-turn refresh from base speed, and manual actor selection without silently resetting spent economy.
- `phase09RealTurnRuntimeAdapter.test.ts` proves the production adapter starts initiative, advances turns, preserves spent Action/movement/Reaction through later snapshots, reconciles atomic action commits and compensating Undo, refreshes economy on round wrap, admits newly instantiated Combatants without restarting initiative, and returns to Freeform when initiative ends.
- `ProductionPlayScreen.tsx` exposes the real `startInitiative`, `endTurn`, and `endInitiative` commands, renders `scene.round` plus the current Actor, and gates Player turn-ending to the authoritative current Character.
- `realTurnRuntimeService.ts` delegates ordering to `domain/initiative`, begin-turn economy to `domain/turnEconomy`, and lifecycle transitions to `resolvePendingResolution` with `end-turn` and `begin-turn` operations rather than maintaining a parallel turn engine.
- In UI run `33498144567`, the Phase 09 real mechanics group containing both focused files and the full TypeScript/production build completed successfully.
- Subsequent canonical merges PR #199 (`ca6b42c47409ff7096f31aeab12624336225ab8f`) and PR #200 (`9c9ef85e4b00aff92e33c8c0a1429209687db016`) changed only V1 evidence/current documentation, so the verified turn-runtime product paths are inherited unchanged.

Product-code change: none.
