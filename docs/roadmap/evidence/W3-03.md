# W3-03 — Attack, damage, healing, and atomic HP writeback

- Gate: `W3-03`
- Classification: `REUSE_LOCKED`
- Verification target: `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`
- Evidence workflow: GitHub Actions `UI` run `33498144567`, job `99824979798`
- Result: PASS by inherited production authority; no product-runtime change required

## Existing production authority reused

The shipped Phase 09 path already owns this Gate:

- `src/app/realAttackTransactionService.ts` composes targeting, attack resolution, damage/HP writeback, and Action economy into one authoritative domain transaction.
- The runtime attack adapter/fact-provider path supplies authored attack facts and explicit mapless or module-provided targeting authority without fabricating spatial facts.
- `src/app/realAtomicHealingTransactionService.ts` composes HP recovery, economy spend, and class-resource spend into one authoritative transaction.
- Both attack and healing project committed `ResolutionEvent` state changes into Activity and use event-native compensating Undo rather than a second writeback path.

No content-name-specific resolver or parallel attack/healing engine is needed for this Gate.

## Exact-SHA verification

GitHub Actions job `99824979798` checked out exact product SHA `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff` and ran the existing W3 verification set, including these Gate-owned files:

```text
tests/ui/phase09RealAttackTransactionService.test.ts
tests/ui/phase09RealAtomicAttackAdapter.test.ts
tests/ui/phase09RealHealingAdapter.test.ts
```

The W3-03 subset contains 23 focused tests. The combined W3 exact-SHA invocation completed `54/54` tests with `0` failures, then `npm run build` completed successfully.

Verified behavior includes:

- hit and miss resolution through the authoritative attack transaction;
- critical hits double damage dice while leaving flat modifiers single;
- damage and Action economy commit atomically with HP writeback;
- rejected range, fabricated spatial-fact, or preview-drift paths do not partially mutate HP/economy;
- supplied runtime effects participate in damage and concentration consequences inside the same authoritative transaction;
- mapless attacks remain valid without invented distance/visibility/cover facts while module-provided spatial authority is enforced when present;
- event-native Activity and Undo preserve/reverse committed HP and economy, with drift rejection for stale Undo;
- runtime Combatant attacks and renamed/unknown external action IDs use the same generic path;
- Second Wind-style healing commits HP, Bonus Action, and class-resource spend atomically, rejects all-or-nothing when the resource cannot be spent, and reverses through event-native Undo;
- renamed/unknown external self-healing action IDs retain the same semantics.

## REUSE_LOCKED decision

No current-HEAD failure was reproduced. The required behavior is already implemented by the existing Common Play/Resolver composition and passed the exact-SHA automated evidence above, so product code remains locked for `W3-03`.

Rendered Windows/Tauri local-session journey evidence remains owned by `W3-08`; structural exact-SHA evidence here does not claim to close that later rendered Gate.
