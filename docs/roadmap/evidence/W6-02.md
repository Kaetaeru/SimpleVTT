# W6-02 Evidence — XP grant and immediate level-up credit ownership

Status: **PASS — REUSE_LOCKED repair + exact-SHA AUTO evidence**

## Acceptance

W6-02 freezes the existing Campaign/Character progression ownership path for:

- MP-E04: DM grants exact XP to one or multiple Characters without a reason field; the durable Campaign advancement owner records the exact value and Session projection can refresh from that owner.
- MP-E05: DM grants one immediate level-up credit; the existing canonical level-up completion path consumes the credit and persists the resulting Character/roster level state.

## Reproduced reachability gap and smallest repair

The canonical Evidence Card recorded that the runtime owner already existed but the real Host/live Session UI had no production control that invoked `grantCampaignAdvancement`. The smallest repair was therefore production reachability only:

- `src/ProductionSessionAdvancementPanel.tsx` exposes Host/live XP and one-credit grants for active Campaign Character roster members.
- `src/ProductionSessionWorkspaceBridge.tsx` mounts that panel in the existing production Session workspace.
- The panel calls the existing AppProvider `grantCampaignAdvancement` owner; it does not introduce a second progression store, advancement engine, transport message, or Character write path.
- The existing `campaign-level-up-complete` / `consumeCampaignLevelUpCredit` path remains the owner for credited Character level-up completion.

PR #294 contains the minimal product repair plus one structural reachability regression and the focused verification workflow.

## Exact-SHA verification

Verification SHA: `a72387016fec255674b8132b1f8b80b08d99da25`

Command owner: `W6-02 AUTO Verification`

Focused command:

```text
npx tsx --test tests/ui/campaignSystems.test.ts tests/ui/productionSessionAdvancementReachability.test.ts
npm run build
```

Focused tests:

- `tests/ui/campaignSystems.test.ts` — 12 tests covering durable Campaign systems including exact multi-member XP, no reason field, one level-up credit, credit consumption, roster level update, and rejection of a missing roster member.
- `tests/ui/productionSessionAdvancementReachability.test.ts` — 1 test proving the real production Host/live workspace mounts the existing advancement owner and keeps level-up credit amount fixed at one.

GitHub Actions:

- Run: `33703181522`
- Job: `100486658419`
- Result: `13/13 PASS`, `0 FAIL`; production build PASS.
- Artifact: `9874278799` (`W6-02-AUTO-9a07c28309ffe781d4ed1e4cea33f7e8f0706577`)
- Artifact SHA-256: `abc109aca9d519e96aec8e03d442a2e071151c659e658a27b99207607f93fc0c`

The artifact name uses the pull-request synthetic merge SHA from `github.sha`; authoritative product verification identity is the workflow run's `head_sha`, `a72387016fec255674b8132b1f8b80b08d99da25`.

## Canonical integration

PR #294 merged the verified repair into `work/v1-composite` as merge commit `b11f5267121c2c4dfb11176ef6ff12841f3c877b`. The merge has verification SHA `a72387016fec255674b8132b1f8b80b08d99da25` as its second parent, so the tested product tree is integrated without reimplementation.

## Closure

W6-02 acceptance is PASS by the reproduced production reachability repair plus exact-SHA automated evidence. This Gate does not by itself close later Windows/rendered multiplayer acceptance. After official ledger reconciliation, the first non-PASS Gate is `W6-03`.
