# W7-07 — Keyboard, screen reader, narrow desktop, Reduced Motion, and correlated diagnostics

Status: PENDING (AUTO owners identified; exact-SHA run not yet recorded)
Classification: REUSE_LOCKED
Scenario mapping: MP-I01, MP-I02, MP-I03, MP-I04, MP-I05, MP-I06 (MP-10 AUTO slice)
Verification head: `PENDING`
Actions checkout: `PENDING`
Run/job: `PENDING`
Focused tests: PENDING (TypeScript owners plus two Rust transport intake tests)
Production build: PENDING
Artifact: `W7-07-AUTO-<verification sha>`

## Reused owners

- `productionPlayWorkspaceAccessibility.test.ts`: sheet and play screens own keyboard focus, icon-only actions stay labeled, disabled hotbar capabilities remain focusable and explain why they cannot run, recovery stays user-visible.
- `sessionResponsiveKeyboardFocusStructure.test.ts`: Escape closes only the top Session layer, dismissals restore focus to their launcher, narrow/constrained desktop keeps Actor Board, Command Center, utilities, result, and reconnect controls reachable.
- `sessionResolutionAnnouncementStructure.test.ts`: the Session result layer exposes one coherent live-region announcement (actor, action, targets, dice, total, outcome, state change) built from the same authoritative ResolutionView the visuals use.
- `visualDiceStructure.test.ts`: physics dice respect `prefers-reduced-motion` with an explicit legacy fallback; no-roll/zero-dice stages never force cinematic dice.
- `physicsDice3DStructure.test.ts`: connected replay completes inside 1.5 seconds and converges to the authoritative faces without changing game state.
- `connectedThreePeerPresentation.test.ts` / `connectedThreePeerDuplicateEventBatch.test.ts`: acting and observing Clients receive the identical ordered presentation and terminal state; duplicate/late batches never lose or reorder later events.
- `connectedDiagnosticCorrelation.test.ts`: H/P1/P2 Activity diagnostics carry the same session-scoped event id, sequence, and resolution id, report applied event counts rather than raw payloads, and converge cursors.
- `session_transport::tests::slow_peer_queue_does_not_block_fast_peer_broadcast` / `peer_outbound_queue_preserves_order_when_drained` (Rust, `transport-intake` job): a slow peer never blocks broadcast intake and drained queues preserve order.

## Scenario acceptance map

- **MP-I01** — keyboard-only action, target selection, interrupt response, result inspection, and Escape layering.
- **MP-I02** — one coherent screen-reader announcement per shared result.
- **MP-I03** — narrow desktop keeps every Session surface reachable.
- **MP-I04** — each peer applies its own motion preference while shared mechanics stay identical.
- **MP-I05** — a multi-die shared roll neither freezes network intake nor drops later ordered events.
- **MP-I06** — logs/Activity correlate session, event, request, and resolution ids without private payloads.

## Exact-SHA verification

PENDING — fill from the `W7-07 AUTO Verification` run (both jobs) on one integration-derived SHA: run/job ids, TAP pass count, Rust test result, production build, artifact id and digest.

The Windows representative accessibility case (MP-I01/I03 WIN) is scheduled under W7-08, per the roadmap.
