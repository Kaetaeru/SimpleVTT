# W3-06 — Conditions, durations, rests, and daily-resource recovery

- Gate: `W3-06`
- Classification: `REUSE_LOCKED`
- Verification target: canonical `work/v1-composite` HEAD `36dcd1ab4c9b51cbfea25596e255455e8e797944`
- Inherited product SHA: `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`
- Evidence workflow: GitHub Actions `UI` run `33498144567`, job `99824979798`
- Result: PASS by current-head verification of unchanged product/test trees plus green lifecycle/rest production journeys; no product-runtime change required

## Current-head tree verification

The trusted product SHA and the live canonical HEAD have identical product and test subtrees:

```text
src/   f2b26668eefcebe1b56f9d7eb86ea9b08ac396cd
tests/ d3cb39f6fe0f71257b7ba9d574d9966e4317cd5e
```

Only the documentation tree differs. This makes the green exact-SHA lifecycle/rest execution directly applicable to the current canonical product and tests.

Focused file checks also match byte-for-byte across the trusted SHA and current HEAD:

```text
tests/domain/phase05Lifecycle.test.ts                 dbd7f7bf870354d85339da04aaad37bf927bc605
tests/domain/resourceRecoveryLockout.test.ts         a1c5c1a712264e428a408e7d749f4857d5ed8fdc
tests/ui/connectedLongRestPreflight.test.ts          fed7ac981de91aba71e8ec8cf553d095a0a54114
tests/ui/connectedLongRestHostRestartRecovery.test.ts 70a18e3901bcdc42457792df605f2282d03fc0be
```

## Lifecycle and resource state-machine evidence

`tests/domain/phase05Lifecycle.test.ts` verifies the real state-machine primitives required by this Gate:

- timed, turn-boundary, rest-bound, and concentration-bound effect expiration;
- SRD condition semantics independent of content identity;
- temporary HP lifecycle and Long Rest cleanup;
- Short Rest Hit Die healing and short-rest resource recovery;
- Long Rest HP, Hit Dice, spell-slot/resource recovery, temporary HP cleanup, and Exhaustion reduction.

`tests/domain/resourceRecoveryLockout.test.ts` verifies authoritative recovery cadence and persistence semantics:

- Short-Rest and Long-Rest recovery locks are independent;
- multi-rest lockouts decrement durably and recover only on the qualifying rest;
- invalid recovery operations reject atomically;
- resource recovery is emitted into the authoritative event stream for connected projection and Undo;
- temporary resource capacity normalizes on Long Rest.

## Production-journey evidence

The green exact-SHA UI job explicitly ran `npm run test:campaign-rest`, whose command executes the production-facing Long Rest transaction suite including:

```text
tests/ui/characterLongRestProjection.test.ts
tests/ui/longRestCompoundCoordinator.test.ts
tests/ui/longRestCompoundRuntimeAdapter.test.ts
tests/ui/connectedLongRestPreflight.test.ts
tests/ui/connectedLongRestTransactionState.test.ts
tests/ui/connectedLongRestWire.test.ts
tests/ui/connectedLongRestOwnerPersistence.test.ts
tests/ui/connectedLongRestCampaignPersistence.test.ts
tests/ui/connectedLongRestRuntimePort.test.ts
tests/ui/connectedLongRestHostRestartRecovery.test.ts
```

The same job also ran the rules-domain suite containing `phase05Lifecycle.test.ts`, `resourceRecoveryLockout.test.ts`, and `wizardLongRestPreparation.test.ts`. The job completed green with no failed test suite.

Current production-facing checks confirm that a connected Long Rest requires exact Character/Campaign revision and owner authority before commit, rejects stale/invalid preflight without partial distributed preparation, and recovers durable host coordination correctly across restart with idempotent global commit/abort handling.

## REUSE_LOCKED decision

No current-head failure or reachability gap was reproduced. Conditions, durations, Short Rest, Long Rest, and daily-resource recovery are already owned by the existing state machine, and the current `src/` and `tests/` trees are byte-identical to the green evidence target. Therefore `W3-06` requires evidence-only closure and no product-code change.

Rendered complete-session Windows/Tauri evidence remains owned by `W3-08`; this Gate claims lifecycle/rest correctness and production transaction journeys, not the later rendered release journey.
