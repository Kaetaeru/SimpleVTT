# W3-06 — Conditions, durations, rests, and daily-resource recovery

- Gate: `W3-06`
- Classification: `REUSE_LOCKED`
- Verification target: canonical `work/v1-composite` HEAD `fbf2a8f26c11dadefa2c426c1a8b990ca7b830dd`
- Inherited product SHA: `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`
- C9 verified source SHA: `b91fb214c7bfe09ee1205aaa4641e0d44de36e9c`
- Evidence workflows: GitHub Actions `Rules Domain` run `33366614575`, job `99408475514`; `UI` run `33366614507`; trusted W3 `UI` run `33498144567`, job `99824979798`
- Result: PASS by inherited exact-SHA lifecycle/resource evidence plus unchanged current product/test trees; no product-runtime change required

## Current-head tree verification

The trusted product SHA and the live canonical HEAD have identical product and test subtrees:

```text
src/   f2b26668eefcebe1b56f9d7eb86ea9b08ac396cd
tests/ d3cb39f6fe0f71257b7ba9d574d9966e4317cd5e
```

Only documentation/roadmap evidence changed after the trusted W3 product SHA. The earlier C9 verified source SHA is an ancestor of that product SHA; comparison from `b91fb214...` to `1a2a5e92...` does not change the W3-06 lifecycle/resource tests or their owning runtime paths. This makes the green C9 lifecycle/resource execution and the later trusted W3 UI execution applicable to the current canonical product and tests.

Focused file checks remain byte-for-byte stable across the trusted SHA and current HEAD:

```text
tests/domain/phase05Lifecycle.test.ts                  dbd7f7bf870354d85339da04aaad37bf927bc605
tests/domain/resourceRecoveryLockout.test.ts          a1c5c1a712264e428a408e7d749f4857d5ed8fdc
tests/ui/connectedLongRestPreflight.test.ts           fed7ac981de91aba71e8ec8cf553d095a0a54114
tests/ui/connectedLongRestHostRestartRecovery.test.ts 70a18e3901bcdc42457792df605f2282d03fc0be
```

## Lifecycle and resource state-machine evidence

The C9 verified source SHA `b91fb214...` passed all 13 repository workflows. Its `Rules Domain` run `33366614575`, job `99408475514`, explicitly completed the domain verification, C9 Gate N coverage check, TypeScript check, and production build.

The executed Rules Domain suite includes `phase05Lifecycle.test.ts`, `resourceRecoveryLockout.test.ts`, and the spell/class resource recovery suites. Together they verify:

- timed, turn-boundary, rest-bound, and concentration-bound effect expiration;
- SRD condition semantics independent of content identity;
- temporary HP lifecycle and Long Rest cleanup;
- Short Rest Hit Die healing and short-rest resource recovery;
- Long Rest HP, Hit Dice, spell-slot/resource recovery, temporary HP cleanup, and Exhaustion reduction;
- independent Short-Rest/Long-Rest recovery locks and durable multi-rest lockout decrement;
- atomic rejection of invalid recovery operations;
- authoritative recovery events usable by connected projection and Undo;
- Long-Rest normalization of temporary resource capacity.

Supplemental exact-source production tests also exercise portable effect durations, short/long-rest expiry, production-clock expiry and Undo, condition/exhaustion state, and rest-specific spell configuration. These files are implementation mapping evidence; this card does not claim they were individually invoked by a workflow unless listed by that workflow.

## Production-path evidence

The trusted W3 `UI` run `33498144567`, job `99824979798`, completed green against product SHA `1a2a5e92...`. Its explicit UI commands cover the production Character Long-Rest projection plus the Phase 09 turn/effect/concentration paths used to keep HP/resources, action economy, concentration, readiness, and active effects coherent.

The earlier C9 `UI` run `33366614507` also completed green on the verified C9 source SHA. It exercises production lifecycle, Ready/concentration, authoritative spellcasting, Phase 09 turn runtime, effect/concentration Undo, and runtime effect application. The C9 source-to-trusted-product comparison leaves the W3-06 lifecycle/resource tests and owning runtime unchanged.

This evidence intentionally does **not** claim that UI job `99824979798` ran a `test:campaign-rest` command or the Rules Domain workflow. Rules Domain execution is anchored separately to run `33366614575` / job `99408475514`.

## REUSE_LOCKED decision

No current-head failure or reachability gap was reproduced. Conditions, durations, Short Rest, Long Rest, and daily-resource recovery are already owned by the existing state machine, and the current `src/` and `tests/` trees remain byte-identical to the trusted W3 product target. Therefore `W3-06` requires evidence-only closure and no product-code change.

Rendered complete-session Windows/Tauri evidence remains owned by `W3-08`; this Gate claims lifecycle/rest state consistency and production-path correctness, not the later rendered release journey.
