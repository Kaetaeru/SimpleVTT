# W3-06 — Conditions, durations, rests, and daily-resource recovery

- Gate: `W3-06`
- Classification: `REUSE_LOCKED`
- Verification target: canonical `work/v1-composite` HEAD `36dcd1ab4c9b51cbfea25596e255455e8e797944`
- Inherited product SHA: `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`
- Evidence workflows: Rules Domain run `33342898384`, job `99341612326`; UI run `33498144567`, job `99824979798`
- Result: PASS by inherited exact-SHA domain and production-path evidence; no product-runtime change required

## Current-head tree verification

The trusted product SHA and the live canonical HEAD have identical product and test subtrees:

```text
src/   f2b26668eefcebe1b56f9d7eb86ea9b08ac396cd
tests/ d3cb39f6fe0f71257b7ba9d574d9966e4317cd5e
```

GitHub compare `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff...36dcd1ab4c9b51cbfea25596e255455e8e797944` contains documentation-only changes. The current canonical product and tests therefore inherit the trusted product SHA unchanged.

Focused file checks also match byte-for-byte across the trusted SHA and current HEAD:

```text
tests/domain/phase05Lifecycle.test.ts                 dbd7f7bf870354d85339da04aaad37bf927bc605
tests/domain/resourceRecoveryLockout.test.ts         a1c5c1a712264e428a408e7d749f4857d5ed8fdc
tests/ui/connectedLongRestPreflight.test.ts          fed7ac981de91aba71e8ec8cf553d095a0a54114
tests/ui/connectedLongRestHostRestartRecovery.test.ts 70a18e3901bcdc42457792df605f2282d03fc0be
```

## Lifecycle and resource state-machine evidence

The Rules Domain workflow at source/check SHA `6095fe8adbe52b047ef5f6fee3413975bf02c3e0` completed successfully in run `33342898384`, job `99341612326`. Its checked-in workflow explicitly executes the W3-06 domain owners, including:

```text
tests/domain/phase05Lifecycle.test.ts
tests/domain/effectTermination.test.ts
tests/domain/effectTurnActivity.test.ts
tests/domain/c9FamilyPEffectExpiredTrigger.test.ts
tests/domain/c9FamilyNEffectLifecycleAcceptance.test.ts
tests/domain/c9FamilyMConditions.test.ts
tests/domain/resourceRecoveryLockout.test.ts
tests/domain/wizardLongRestPreparation.test.ts
```

That workflow also runs typecheck, the V1 mechanism coverage check, legacy-execution-boundary audit, and the production build. GitHub compare `6095fe8adbe52b047ef5f6fee3413975bf02c3e0...1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff` contains no `src/domain/` changes and no changes to the W3-06 domain tests above, so this green domain result is inherited by the trusted product SHA.

The domain coverage proves the state-machine semantics required by this Gate:

- timed, turn-boundary, rest-bound, and concentration-bound effect expiration;
- generic condition semantics independent of content identity;
- Short Rest Hit Die healing and short-rest resource recovery;
- Long Rest HP, Hit Dice, spell-slot/resource recovery, temporary HP cleanup, and Exhaustion reduction;
- independent Short-Rest and Long-Rest recovery lockouts;
- durable multi-rest lockout decrement and recovery only on the qualifying rest;
- atomic rejection of invalid recovery operations;
- authoritative resource recovery events suitable for projection and Undo;
- temporary resource-capacity normalization on Long Rest.

## Exact product-SHA production-path evidence

The trusted product SHA `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff` has a successful UI frontend check in run `33498144567`, job `99824979798`. The exact `ui.yml` at that SHA explicitly executes production-facing W3-06 coverage including:

```text
tests/ui/connectedActorArtifactLifetimeProduction.test.ts
tests/ui/connectedActorArtifactLifecycleProduction.test.ts
tests/ui/characterLongRestProjection.test.ts
```

Those tests verify that elapsed/durable artifact lifetime is authoritative and identity-invariant, connected replay/reconnect/duplicate handling and Undo converge on the same artifact state, and Character Long Rest projection restores HP and declared long-rest resources while clearing temporary state without rewriting source identity or inventing resurrection. The same UI job finishes with the production build green.

A supplemental resource-specific source/check SHA `8f4ac12dcd8965962f2b636be2901321b4f06599` also completed both Rules Domain run `33307482938`, job `99246470393`, and Resource Economy run `33307482939`, job `99246470394` successfully. The resource owners `src/domain/resources.ts` and `src/domain/resolutionRestOps.ts` were already at their later inherited implementations before that check, and the later `6095fe8...` Rules Domain run revalidated `resourceRecoveryLockout.test.ts` after the final `src/domain` changes.

## REUSE_LOCKED decision

No current-head failure or reachability gap was reproduced. Conditions, durations, Short Rest, Long Rest, and resource recovery are already owned by the existing state machine and production adapters, with green domain and production-path evidence inherited unchanged by the current canonical product. Therefore `W3-06` requires evidence-only closure and no product-code change.

Rendered complete-session Windows/Tauri evidence remains owned by `W3-08`; this Gate claims lifecycle/rest correctness and production-path state consistency, not the later rendered release journey.
