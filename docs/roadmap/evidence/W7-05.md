# W7-05 — DM-only, hidden, and private payload privacy

Status: PASS
Classification: REUSE_LOCKED → contract/reachability gap reproduced → focused repair (Evidence Card, PR #319) → verified
Scenario mapping: MP-B05, MP-B06, MP-B07 (MP-09 AUTO + Windows slice)
Verification head: `7577c454540c95a186ed7f7a43f8c4bbba178aac`
Actions checkout: `7577c454540c95a186ed7f7a43f8c4bbba178aac` (push to `work/v1-composite`, merge of PR #321)
Run/jobs: `33886690863` / `101072045209` (verify) / `101072044329` (windows-hidden-roll-disclosure)
Focused tests: 18/18 PASS + production build PASS
Windows H+P1+P2: PASS
Artifacts:
- `9942194514`, `W7-05-AUTO-7577c454540c95a186ed7f7a43f8c4bbba178aac`, `sha256:f381a29aa8682ff91fbea0ad90af22d683e983ab62483ccae99bdbfe3203ce5f`
- `9942781222`, `W7-05-WIN-7577c454540c95a186ed7f7a43f8c4bbba178aac`, `sha256:9a6b94b4d012b51776ec98716526888f98c9047905d71beb294a5fd24a9ea984`

## Repair on the existing path (PR #321)

The Evidence Card in PR #319 reproduced that the connected presentation contract could only carry a `public` audience and that no ordered disclosure event existed. The repair extended the existing owners only:

- `ConnectedResolutionPresentationV1.audience` gains `{scope:"public-redacted",hidden:("roll"|"targets")[]}`; the Host redacts an already-built envelope and the receiver-side validator rejects any envelope that still leaks a hidden fact.
- `ConnectedEventPayload` gains `kind:"resolution-disclosure"`, committed through `HostSessionLedger.commitHostEvent` and fanned out on the existing `event-batch` wire; the connected wire validator accepts and validates the new payload (the Windows run reproduced the missing validator branch before the fix).
- The committed hidden `resolution` event keeps structured state changes and redacts summary/provenance/result text; catch-up readers replay redacted history.
- Host-only controls: `setNextResolutionVisibility()` arms the next Resolution from the Command Center; `discloseResolution()` reveals selected facts from the Activity pane.

## Reused owners (AUTO, 18/18)

- `connectedHiddenResolutionPrivacy.test.ts` (5): MP-B05 Host keeps private detail while P1≡P2 hold the public projection; MP-B06 live, committed, and catch-up payloads carry neither raw values nor inferable metadata; MP-B07 one ordered disclosure, duplicate-safe, only the selected fact, decoded through the real wire codec; receiver-side leak rejection; per-Resolution arming.
- `connectedResolutionPresentation.test.ts`, `connectedThreePeerPresentation.test.ts`: the public envelope strips private interrupt controls and action metadata; identical ordered fan-out to acting and observing Clients.
- `sessionImageHandoutRuntimeAdapter.test.ts`, `campaignDmLibraryNoteStructure.test.ts`, `campaignDmLibraryStructure.test.ts`: handout reveal/withdraw/reconnect privacy and DM Library private metadata never enter Session projection.

## Windows H+P1+P2 observation (`scripts/run-tauri-e2e-w7-05.mjs`, `-W705`)

1. **MP-B06** — the DM arms 비공개 굴림 in the Command Center and resolves a check; P1 and P2 render `비공개` and their rendered text contains none of the Host's dice, total, or outcome markers.
2. **MP-B05** — the Host's own Activity pane renders the private detail; P1 and P2 hold the identical public Activity projection; the Host tracks the hidden Resolution with nothing disclosed.
3. **MP-B07** — the DM clicks 판정 결과 공개 in the Activity pane; P1 and P2 receive the disclosed dice in order after the hidden Resolution, render the `DM 공개` entry with the disclosed outcome in their Activity panes, and the disclosed fact is not offered again on the Host.

Screenshots and body text for every step, plus `w7-05-summary.json`, are in the WIN artifact.

## Scenario acceptance map

- **MP-B05** — public and DM-only Activity: Host private detail retained, Clients receive only the permitted public projection.
- **MP-B06** — hidden roll/target facts omit raw values and inferable metadata on unauthorized Clients, live and in committed history.
- **MP-B07** — one new ordered disclosure event reveals only the selected result, exactly once.

## Notes

The first Windows job on this SHA failed before any scenario step (`No window could be found` at WebDriver session creation, a start-up race); the re-run of the same job on the same SHA passed and is the recorded evidence. The runner retries session creation for a bounded time from PR #328 onward.
