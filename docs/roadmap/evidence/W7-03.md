# W7-03 Evidence — Host restart + session-end cleanup safety

Status: **PASS**  
Classification: `REUSE_LOCKED`  
Scenario mapping: `MP-A08`, `MP-A09`

## Scope

W7-03 reuses the existing production Session-end, AppProvider stop-refresh, Session lifecycle, participant lifecycle, turn projection, connected presentation, Party Stash approval ownership, and Ready/Concentration owners. No second cleanup/restart state machine or generic cleanup suite was added.

No current-HEAD product/runtime or existing test-implementation failure was reproduced, so no `src/` or existing test implementation file changed.

## Exact-SHA verification

- Verification branch head: `4986833eb20590ec486721ef6f45b86c2b3cb021`
- GitHub Actions pull-request checkout: `9ae9615e5cf1c0377193e90e1918a9d728dadc1f`
- Workflow: `W7-03 AUTO Verification`
- Run: `33740111205`
- Job: `100599791508`
- Focused W7-03 tests: **PASS**
- Production build: **PASS**
- Legacy Execution Boundary run `33740111146`: **success**
- Contract validation run `33740110937`: **success**
- Artifact: `9887399132` — `W7-03-AUTO-9ae9615e5cf1c0377193e90e1918a9d728dadc1f`
- Artifact digest: `sha256:a1edb9d24fd85deb525830ae142f562aae29722bdf58a4ef89e1281b08694ea2`

Focused command:

```bash
npx tsx --test \
  tests/ui/productionSessionEnd.test.ts \
  tests/ui/appProviderStopSessionRefresh.test.ts \
  tests/ui/productionSessionLifecycleAdapter.test.ts \
  tests/ui/productionParticipantLifecycle.test.ts \
  tests/ui/connectedTurnProjection.test.ts \
  tests/ui/connectedResolutionPresentation.test.ts \
  tests/ui/connectedPartyStashApprovalOwnerTransfer.test.ts \
  tests/ui/c9FamilyOReadyConcentrationProduction.test.ts
```

## Scenario ownership

- `MP-A08`: `productionSessionEnd` proves the Host publishes `session-ended` before transport teardown; Host and Clients enter explicit offline/ended state; participants, projections, resolution/transient authority, Initiative/turn state, and reconnect state are cleared while durable Character state survives. `appProviderStopSessionRefresh` proves the final authoritative offline snapshot wins transport publication races.
- `MP-A09`: the same production Session-end owner immediately starts a fresh Host Session with a new Session ID and verifies stale participants, projections, prior Initiative/round state, and transient economy do not return. The participant, turn, presentation, approval, and Ready/Concentration owners remain in the focused exact-SHA run so stale requester/queue/presentation ownership stays covered by the existing production paths rather than a parallel cleanup implementation.

## Closure

W7-03 is verified PASS under `REUSE_LOCKED`. Product/runtime and existing test implementations remain unchanged; only the scoped verification workflow and evidence record were added. The official ledger/current records must advance W7-03 to PASS before this gate is canonically closed.
