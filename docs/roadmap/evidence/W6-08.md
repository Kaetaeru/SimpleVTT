# W6-08 Evidence — Windows H+P1 representative DM live operations

Gate: `W6-08`  
Classification: `VERIFY_ONLY`  
Result: **PASS**

## Acceptance

The existing Windows Tauri H+P1 product path must verify the representative Journey J5 DM live-operation flow across `MP-E~G`: grant/revoke, Party Stash, distributed Long Rest, and image handout behavior. Final P2 observer-parity acceptance remains reserved for `W9-02`.

## Exact verification

- Verification head: `36a9848a025f078f60b956e05f3432cbf5b14da4`
- Pull-request checkout: `db4d81f521cf15774dcff13d3249186f0c19dde1`
- GitHub compare `36a9848a025f078f60b956e05f3432cbf5b14da4...db4d81f521cf15774dcff13d3249186f0c19dde1`: **0 changed files**; the synthetic checkout therefore carries the same verification tree.
- Workflow: `W6-08 Tauri Verification`
- Run: `33716559390`
- Job: `100526862083`
- Journey command: `npm run test:e2e:tauri -- --w608`
- Production build: `npm run build`
- Result: Windows Tauri H+P1 Journey J5 **PASS**; production build **PASS**.

## Rendered behavior proved

The real Tauri UI journey uses the existing production paths and proves, in one connected H+P1 session:

1. DM Character GP grant reaches the Player and Host owner projection.
2. Player deposits GP into the existing Party Stash transaction path and both sides converge.
3. DM GP revoke reaches the Player without a parallel inventory path.
4. Image handout reveal and withdraw use the existing connected handout projection path.
5. Distributed Long Rest uses the existing Character+Campaign rest path and advances the enabled Campaign calendar by 8 hours while completing the connected owner flow.

The workflow writes rendered screenshots/text plus `w6-08.json` into the Windows artifact. It does **not** claim final P2 observer parity; that remains `W9-02`.

## Artifact

- Artifact ID: `9878842089`
- Name: `SimpleVTT-W6-08-Tauri-36a9848a025f078f60b956e05f3432cbf5b14da4`
- Digest: `sha256:b44dfe4f486409522630c632819665cee4eccdab72077857838876a33ac747b9`

## Change authorization / repair history

No product/runtime defect was reproduced. The failures encountered while constructing this VERIFY_ONLY evidence were confined to the new test harness: missing saved-Character setup, controlled numeric-input replacement, an invalid zero-GP assumption, missing Campaign-calendar setup, and an XPath string incorrectly passed to `document.querySelector`. Those were repaired only in the W6-08 harness. No `src/` product file changed.

## Closure

`W6-08` is PASS. This completes W6 at **8/8 PASS**. The official ledger advances to **75.0/100.0**, **56/72 PASS**, **16/72 PENDING**, with `W7-01` as the first non-PASS Gate.
