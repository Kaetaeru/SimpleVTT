# W6-01 Evidence — DM item/GP grant-revoke and owner projection refresh

Status: **PASS — REUSE_LOCKED exact-SHA AUTO evidence**

## Acceptance

W6-01 freezes the existing DM inventory/currency and Character-owner projection paths for:

- MP-E01: DM catalog/custom item grant reaches durable owner state and permitted Session projection.
- MP-E02: item revoke is deliberate, including explicit equipped-item transition policy.
- MP-E03: GP grant/revoke is exact and overdraft is rejected without partial mutation.
- MP-J05/J06 owner inventory/GP parity: accepted owner mutations carry fresh Character revision identity and inventory membership refreshes through the existing projection path.

## Reused production owners

- `src/app/sessionInventoryRuntimeAdapter.ts`
- `src/app/connectedCampaignSystemsRuntimeAdapter.ts`
- `src/app/characterSessionProjection.ts`
- `src/app/characterSessionProjectionReconstruction.ts`
- `src/app/connectedCharacterProjectionHandshake.ts`
- `src/app/characterSessionProjectionMount.ts`
- `src/app/campaignDmLibraryMaterializationAdapter.ts`

No product/runtime or test implementation change was required. PR #291 added only `.github/workflows/w6-01-auto.yml` to execute the existing owner tests together.

## Exact-SHA verification

Verification SHA: `30606e6b056027a3e10ddbae70f38f428b2714b6`

Command owner: `W6-01 AUTO Verification`

Focused tests:

- `tests/ui/sessionInventoryRuntimeAdapter.test.ts`
- `tests/ui/campaignDmLibraryGrantDurability.test.ts`
- `tests/ui/connectedCampaignOwnerInventoryWire.test.ts`
- `tests/ui/connectedCharacterInventoryProjectionRefresh.test.ts`
- `tests/ui/connectedCustomItemProjection.test.ts`

GitHub Actions:

- Run: `33701452879`
- Job: `100481432959`
- Result: `13/13 PASS`, `0 FAIL`
- Artifact: `9873659413` (`W6-01-AUTO-30606e6b056027a3e10ddbae70f38f428b2714b6`)
- Artifact SHA-256: `bb6031112dd14fedf00aa90485e7583332cdaefd1befe4f223d710e4939e2204`

## Observed coverage

The exact-SHA suite proves that the existing implementation:

- grants catalog items idempotently and revokes the intended quantity;
- rejects equipped-item revoke until the explicit `forceUnequip` transition is requested;
- grants GP exactly, restores the prior wallet through Undo, rejects overdraft, and keeps request-scoped Undo idempotent;
- preserves an already materialized Character item even when DM Library recents persistence fails afterward;
- requires fresh Character source/runtime revision identity for accepted connected owner-inventory results;
- accepts forward inventory-membership refresh while rejecting unrelated source drift;
- projects inert custom Campaign items without inventing executable mechanics and rejects untrusted equipped/wielded/attuned state.

## Closure

W6-01 AUTO acceptance is PASS on the canonical exact SHA. This Gate does not by itself close later Windows/rendered multiplayer acceptance or all of MP-05/#116. The official ledger still needs a separate evidence-only reconciliation before execution advances to W6-02.
