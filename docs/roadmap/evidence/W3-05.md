# W3-05 — Reaction, interruption, concentration, and Ready lifecycle

- Gate: `W3-05`
- Classification: `REUSE_LOCKED`
- Verification target: `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`
- Evidence workflow: GitHub Actions `UI` run `33498144567`, job `99824979798`
- Result: PASS by inherited exact-SHA production authority; no product-runtime change required

## Existing production authority reused

The shipped Common Play runtime already owns the required lifecycle primitives. Reaction interruption is handled by the existing authoritative reaction/action-resolution adapters, Ready-style behavior is represented by stored invocations, and deferred spell concentration is composed with the stored Ready artifact. No content-name-specific reaction, Ready, or concentration engine is required for this Gate.

## Exact-SHA CI evidence

GitHub Actions run `33498144567` is the `UI` workflow at exact product SHA `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`; frontend job `99824979798` completed successfully. The workflow definition at that exact SHA explicitly executes the following W3-05 production-facing tests, and the corresponding job steps are green:

```text
tests/ui/c9FamilyOReadyConcentrationProduction.test.ts
tests/ui/phase09ManualMovementReactionAdapter.test.ts
tests/ui/phase09ConcentrationSaveWorkflow.test.ts
```

Those three files contain 11 focused tests in total.

Verified production-path behavior includes:

- an opportunity-attack reaction is staged without prematurely spending Reaction, commits the Reaction only with the authoritative attack result, records Activity, and event-native Undo restores the state;
- invalid trigger/target facts reject the reaction without spending Reaction or partially mutating HP;
- installed spell Ready captures the action into an authoritative stored-invocation artifact, starts held concentration in the capture transaction, and Undo reverses the capture;
- Ready behavior is structural and rename-invariant, including non-spell feature capture without incorrectly starting concentration;
- triggering a readied spell removes its stored invocation and ends held concentration, while Undo restores both;
- concentration damage pauses for explicit authoritative d20 input without partially committing damage or action economy;
- a successful concentration save commits damage while preserving concentration, a failed save removes the concentration group/effect and projects raw Activity, and Undo restores all state;
- stale concentration input is rejected without discarding an intervening authoritative mutation.

## Supplemental domain evidence

The same exact product SHA also contains focused domain coverage for the underlying generic mechanisms:

```text
tests/domain/commonPlayReactionRuntime.test.ts
tests/domain/commonPlayStoredInvocationRuntime.test.ts
tests/domain/c9FamilyODeferredConcentration.test.ts
tests/domain/phase05Lifecycle.test.ts
```

These tests map the reusable reaction interceptor, stored-invocation, deferred concentration, concentration replacement, payment/refund, replay, and cancellation semantics underneath the production-facing adapters. They are supporting source/test evidence; the PASS decision above is anchored to the production-facing files explicitly executed by the green exact-SHA UI workflow.

## Current-HEAD inheritance check

GitHub compare `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff...e1795403852583e29c32ba947084deeaef19b3e3` contains no `src/` or `tests/` changes. The canonical integration HEAD therefore inherits the exact-SHA W3-05 implementation and tests unchanged.

## REUSE_LOCKED decision

No current-HEAD failure was reproduced. The required reaction, interrupt, concentration, and Ready lifecycle behavior already exists on the production path and has exact-SHA automated evidence, so product code remains locked for `W3-05`.

Rendered complete-session Windows/Tauri evidence remains owned by `W3-08`; this structural/production-path Gate does not claim to close that later rendered Gate.
