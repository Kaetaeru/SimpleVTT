# W1-01 Evidence Card

Gate ID: `W1-01`

Acceptance criterion: First run reaches Character Library and the Character creation entrypoint.

Production entrypoint: `src/App.tsx` -> `V1HomeScreen` -> `CharacterLibraryScreen` / `CharacterCreateScreenV10`.

Existing implementation files:
- `src/App.tsx`
- `src/V1HomeScreen.tsx`
- `src/CharacterCreateV10.tsx`
- `src/FirstRunTutorialBridge.tsx`

Existing automated tests:
- `tests/ui/firstRunTutorialStructure.test.ts`
- `tests/ui/v1ProductShellStructure.test.ts`

Existing Tauri/Windows evidence: Not required for this `REUSE_LOCKED` gate. Rendered Tauri verification starts with the `VERIFY_ONLY` W1 gates (`W1-05` through `W1-08`).

Exact observed failure: None on the current integration-derived branch.

Smallest required change: No product-code change. Add a focused regression assertion that the real product starts at Home and exposes both the Character Library route and Guided Create route.

## Exact evidence

- Base integration SHA: `4cd28375e7ee4ca561f1d1430b637d0867b04165`
- Focused evidence SHA: `b0ceb332e197629a2b81ed40c6d33d549c8d658f`
- Pull request: `#191`
- GitHub Actions UI run: `33386511863`
- Required step: `Verify v1 shell, connected topology, continuity, and Session layer contracts`
- Result at evidence capture: `success`

The focused test locks all of these production connections together:
1. `AppRoute` starts at `home`.
2. Home renders `V1HomeScreen`.
3. `내 캐릭터 열기` routes to `characters` and renders `CharacterLibraryScreen`.
4. `새 캐릭터 만들기` creates the existing `guided` draft and routes to `create`.
5. `create` renders the existing `CharacterCreateScreenV10`.

No duplicate Character store, creator, route, or runtime was introduced.