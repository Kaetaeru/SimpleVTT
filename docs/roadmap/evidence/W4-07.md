# W4-07 — Actor materialization / handout / spatial fallback

Gate: `W4-07`
Classification: `REUSE_LOCKED`
Verification product SHA: `2ac28651312f1fdbe82edb74fd13f342a8f910f7`
Current evidence base SHA: `2ffc2004d795a350a4aa3676bcd7d4cb362f1ea1`

## Acceptance mapping

W4-07 fixes the existing Campaign DM Library -> live Session materialization/presentation path for the complete `MP-G01` through `MP-G09` scope without introducing a second DM Library, mutation route, transport, presentation path, or spatial mechanics owner.

- **G01 — NPC quick-add:** the Windows H+P1+P2 journey materializes a Campaign-private NPC definition as a public Session Actor while private source metadata stays absent from Client projections.
- **G02 — PC preset quick-add:** a Campaign-owned PC preset materializes as a DM-controlled Actor without transferring Character ownership to either Player.
- **G03 — custom JSON item/NPC/image:** production parsing preserves validated custom fields/provenance, materializes the imported NPC, and rejects executable/unsafe fields.
- **G04 — handout reveal/withdraw:** Host preview remains private; explicit reveal reaches P1/P2 on the shared presentation path; withdraw removes the reveal from both Clients.
- **G05 — reconnect during active handout:** a real production Client leave/rejoin restores the permitted active reveal, while reconnect after withdraw restores no withdrawn/private asset.
- **G06 — DM-only note/folder/search privacy:** Host-private note/folder/tag/favorite sentinels and DM Library/index shapes are absent from P1/P2 snapshots.
- **G07 — pinned rules/content lookup:** after Session start, ambient H/P1/P2 catalogs may drift, but lookup remains pinned to the captured Session content snapshot and creates no shared mechanics event.
- **G08 — no spatial provider:** range-dependent mechanics remain unconstrained instead of inventing distance; a ranged action is not falsely rejected.
- **G09 — compatible spatial provider:** provider facts retain provenance and Host alone validates dependent range mechanics before authoritative fan-out.

Existing deterministic AUTO coverage is retained, including `tests/ui/sessionImageHandoutRuntimeAdapter.test.ts` for G04-G07. Exact-SHA GitHub Actions UI run `33636100212`, job `100267245818`, completed successfully at `2ac28651312f1fdbe82edb74fd13f342a8f910f7`, including the connected topology/continuity/Session-layer suite and the full frontend build.

## Exact Windows verification

GitHub Actions V1 Tauri Verification run `33636100197`, job `100267245684` (`tauri-w4-07-spatial`), at verification SHA `2ac28651312f1fdbe82edb74fd13f342a8f910f7` completed successfully on Windows.

The job passed both required real-desktop H+P1+P2 steps:

1. `Verify W4-07 G01/G02/G03/G08/G09 in Windows Tauri H+P1+P2` — PASS.
2. `Verify W4-07 G04/G05/G06/G07 in Windows Tauri H+P1+P2` — PASS.

Evidence artifact:

- `SimpleVTT-W4-07-G01-G09-2ac28651312f1fdbe82edb74fd13f342a8f910f7`
- artifact ID `9849024415`
- SHA-256 `b044f73320cd10bf2446695677ca2098c7c162d01190feed7ecc599078c656a0`

The same exact head also passed Contract validation, Legacy Execution Boundary, and UI. PR #249 changed only `.github/workflows/v1-w1-tauri-verification.yml` and `scripts/run-tauri-e2e-w4-07-handout.mjs`; no product/runtime implementation was changed. PR #249 merged the verified tree into canonical `work/v1-composite` as `2ffc2004d795a350a4aa3676bcd7d4cb362f1ea1`; the merge and verified head have no file differences.

## Decision

All `MP-G01` through `MP-G09` required Windows H+P1+P2 acceptance observations pass on one exact verification SHA, with matching deterministic AUTO coverage and an exact artifact digest. No current product/runtime defect remains for this `REUSE_LOCKED` Gate. W4-07 is ready for evidence-ledger/current closure; after reconciliation, MP-07 issue #117 can be closed with the exact SHA/run/artifact record above.
