# W3-04 — Shared spell, feature, and item action transaction path

- Gate: `W3-04`
- Classification: `REUSE_LOCKED`
- Verification target: `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`
- Evidence workflow: GitHub Actions `UI` run `33498144567`, job `99824979798`
- Result: PASS by inherited production authority; no product-runtime change required

## Existing production authority reused

The shipped Phase 09/Common Play path already owns this Gate. Spell, feature, and item actions resolve through the existing reusable runtime/Resolver composition instead of content-name-specific side paths. The shared execution flow owns action/resource or item cost, target validation, effects, committed transaction state, and Activity projection.

No new spell engine, feature engine, item-use engine, or parallel writeback path is required for this Gate.

## Exact-SHA verification

GitHub Actions job `99824979798` checked out exact product SHA `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff` and ran `npm run test:ui` (`vitest run tests/ui`) successfully.

Gate-owned evidence in that exact-SHA run includes:

```text
tests/ui/phase09SpellRuntime.test.ts
tests/ui/phase09SpellTargetModes.test.ts
tests/ui/phase09SpellRuntimeAdversarial.test.ts
tests/ui/phase09ResourceRuntime.test.ts
tests/ui/phase09ResourceRuntimeServices.test.ts
tests/ui/phase09RealActionCostService.test.ts
tests/ui/phase09RealItemCostService.test.ts
```

Verified behavior includes:

- spell actions apply hit-target damage, save-target damage with half-on-save behavior, healing, and self-target AC effects through the runtime path;
- feature/resource execution consumes Action plus Rage transactionally and reuses the shared apply path for Fighter Second Wind;
- unavailable resources are rejected without an invalid spend;
- real action-cost execution spends Action/Bonus Action through `resolveAndApply`, while missing required targets do not spend action resources;
- item use commits canonical inventory consumption only after successful resolution; the exact-SHA item test records `action=use-item`, `cost=item:ration:1`, `target=self`, `effect=consume-item`, and a committed `transactionId` while persisting the count from 2 to 1;
- committed operations therefore carry cost, target, effect, and transaction/Activity semantics through the existing reusable execution boundary rather than a content-specific mutation path.

## Current-HEAD inheritance check

GitHub compare `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff...7fa0e0d90ec88853dcb7ee791e220d0becb3909b` shows only roadmap/current/evidence documentation changes after the verification SHA. No product runtime, tests, package scripts, or workflow paths changed, so the exact-SHA W3-04 product evidence is inherited unchanged by the current integration HEAD.

## REUSE_LOCKED decision

No current-HEAD failure was reproduced. The required spell/feature/item transaction behavior is already implemented and passed exact-SHA automated evidence, so product code remains locked for `W3-04`.

Rendered Windows/Tauri session evidence remains owned by `W3-08`; this structural exact-SHA evidence does not claim to close that later rendered Gate.
