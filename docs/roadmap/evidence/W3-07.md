# W3-07 — Correction/Undo preserves history through compensating events

- Gate: `W3-07`
- Classification: `REUSE_LOCKED`
- Verification target: `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`
- Evidence workflow: GitHub Actions `UI` run `33498144567`, job `99824979798`
- Result: PASS by inherited exact-SHA event-native Undo authority; no product-runtime change required

## Existing production authority reused

The shipped Common Play/Resolver path already treats correction and Undo as event-native compensation rather than destructive history editing. The existing attack/healing/reaction/concentration integrations project committed `ResolutionEvent` changes into Activity and reverse committed state through the same event history.

The W3-03 canonical evidence already records this invariant for the production attack/healing path: committed HP/economy state is reversed by event-native compensating Undo, with drift rejection rather than a second writeback path. W3-05 independently records the same event-native Undo behavior for reaction, Ready, and concentration lifecycle state.

## Exact-SHA verification

GitHub Actions job `99824979798` checked out exact product SHA `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff` and ran the existing W3 production-facing set, including:

```text
tests/ui/phase09RealAtomicAttackAdapter.test.ts
```

That test directly verifies the W3-07 history invariant:

- a committed attack projects `ResolutionEvent` details and state changes into Activity;
- `undoLastResolution()` restores HP and Action economy using event-native Undo even when the legacy before-snapshot path is deliberately disabled;
- the original Activity entry is not deleted: the same Activity id remains present and is marked `reversed`;
- when authoritative scene state has drifted after the committed events, Undo is rejected instead of rewriting or deleting intervening history.

The same exact-SHA production evidence is already inherited by W3-03 and W3-05. Subsequent W3 evidence/closure work has been documentation-only and has not introduced a product-runtime repair for this path.

## REUSE_LOCKED decision

No current-HEAD failure was reproduced. The repository-defined criterion — correction/Undo uses a compensating event rather than history deletion — is directly exercised by the existing exact-SHA production adapter test and is already part of the green W3 UI evidence. Product code therefore remains locked for `W3-07`.

Rendered complete-session Windows/Tauri proof remains owned by `W3-08`; this Gate records UNIT+INT event-history authority only.
