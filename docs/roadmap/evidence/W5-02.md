# W5-02 Trusted Persisted Character Projection Evidence

Gate: `W5-02`  
Classification: `REUSE_LOCKED`  
Scope: `Host-unknown persisted Character creates a trusted owner projection` (`MP-A02`, `MP-B01`, `MP-B02`, `MP-B03`, `MP-B04`)  
Evidence product SHA: `2ac28651312f1fdbe82edb74fd13f342a8f910f7`  
Canonical evidence-base SHA when this record was prepared: `fec5ced3491cf6ba7e2363c8f84e1bd6890e3786`

## Verdict

The existing production connected-session path satisfies the W5-02 trusted persisted-Character projection contract without a product-code repair. A Host-unknown persisted Character is represented as an ephemeral owner projection, client presentation drift cannot become rules authority, projected-player resolution temporarily activates the owner context and restores Host/local context, and local Host control does not consume remote Character ownership.

This focused record is ready for official ledger/current reconciliation. Until that reconciliation commit lands, the authoritative ledger remains `PENDING` for W5-02.

## Automated exact-SHA evidence

GitHub Actions UI run `33636100212`, job `100267245818`, completed successfully at exact product SHA `2ac28651312f1fdbe82edb74fd13f342a8f910f7`.

The Phase 14 connected verification on that job directly passed the production ownership/projection regressions relevant to W5-02:

- `tests/ui/productionLocalCharacterSwitch.test.ts`
  - `switching saved local Characters replaces only the prior local projection and preserves remote SessionProjection authority` passed;
  - the remote Character remains registered as an ephemeral SessionProjection while the local canonical Character changes;
  - `activateProjectedCharacterResolutionContext` temporarily makes the projected Character active for resolution without overwriting its projection-owned action/economy state;
  - `restoreProjectionResolutionContext` restores the prior local/Host context while preserving the projected Character and its ownership registry.
- `tests/ui/productionParticipantLifecycle.test.ts`
  - `Host rejects invalid SessionProjection before creating ghost state` passed;
  - incompatible/mismatched projection identity is rejected before participant, peer, ledger, projection-registry, Scene actor, action, or economy state can be published;
  - accepted participant/reconnect paths remain Host-authoritative and preserve the Character/peer binding.

These exact-SHA PASS results cover the production mount/isolation/context-switch portion of `MP-A02`, `MP-B03`, and `MP-B04` and the rejection boundary needed by `MP-B02`.

## Trusted projection contract on the verified product tree

`tests/ui/characterSessionProjection.test.ts` at the same exact product SHA fixes the source/runtime trust boundary used by the connected path:

- the projection carries declarative source/runtime revisions, source-owned max HP, and qualified class/species/background identities;
- AC and presented attacks are not accepted as source authority;
- Host parsing succeeds only when canonical Host content resolves the exact projected identities;
- unsupported executable/source-authority/content-definition injection fields are rejected;
- changing client presentation AC, proficiency bonus, speed, or attack values leaves the source, source-authority, runtime, and content-identity payloads unchanged;
- runtime HP outside the source-owned max-HP bound is rejected.

`tests/ui/productionParticipantLifecycle.test.ts` routes the same `buildCharacterSessionProjectionV1` / Host parse-and-mount contract through the production connected hello path, so the trust boundary is not a detached test-only model.

Together this establishes `MP-B01` and `MP-B02`: the Host reconstructs from bounded source/runtime facts and canonical content identities rather than trusting client-derived presentation mechanics.

## Scenario mapping

- `MP-A02` — the connected Host mounts the joining persisted Character as a SessionProjection/ephemeral projected Character; the local Character switch regression proves that remote projection identity survives independently of the Host library/local Character.
- `MP-B01` — projection source/runtime and canonical qualified identities are bounded and validated before Host acceptance.
- `MP-B02` — client-derived AC/speed/proficiency/attack drift and injected mechanics do not become trusted authority; malformed/incompatible projections are rejected before ghost state.
- `MP-B03` — projected-player resolution temporarily activates the projected Character context and restores the prior Host/local context afterward.
- `MP-B04` — switching/controlling the local canonical Character removes only the prior local projection and preserves the remote owner projection, its actions/economy, and registry ownership.

## Windows topology evidence inherited on the same product SHA

V1 Tauri Verification run `33636100197` completed successfully at the same exact product SHA. Job `100267245684` (`tauri-w4-07-spatial`) exercised the existing production Windows Tauri H+P1+P2 topology, including replacement-player reconnect, using artifact:

- ID: `9849024415`
- Name: `SimpleVTT-W4-07-G01-G09-2ac28651312f1fdbe82edb74fd13f342a8f910f7`
- SHA-256: `b044f73320cd10bf2446695677ca2098c7c162d01190feed7ecc599078c656a0`

This is reused connected-topology evidence, not a new W5 implementation. Final full rendered multi-window parity remains reserved for the later roadmap verification/release gates.

## Current-tree inheritance check

The canonical branch has advanced from product SHA `2ac28651312f1fdbe82edb74fd13f342a8f910f7` through W4/W5 evidence-only reconciliation work. W5-01 explicitly records that no product/runtime/test repair was introduced in its closure; this W5-02 record likewise changes evidence only.

## Reimplementation decision

- Reproduced current-tree product failure: **none**.
- Production reachability/contract gap requiring repair: **none identified for W5-02**.
- Product-code change permitted by `REUSE_LOCKED`: **no**.
- Next action: reconcile W5-02 as `PASS` in the official evidence ledger/current documents, then advance to W5-03.
