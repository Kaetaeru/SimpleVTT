# W9-03 — Zero-to-Next-Session golden journey

Status: PENDING (Windows runner iterating on PR heads)
Classification: BUILD
Scenario mapping: legacy Journey J1~J9; importer closure slice (W8-04 + W9-03)
Verification head: `PENDING`
Run/job: `PENDING`
Artifact: `W9-03-WIN-<verification sha>`

## Journey (scripts/run-tauri-e2e-w9-03.mjs, real Windows Tauri H + P1)

1. First run on a fresh data root.
2. Import and activate the golden external module (`homebrew.golden-v1`) through the real Contents screen (예제 애드온 불러오기 → 검토 완료 · 설치).
3. Create a Character whose Background is the imported **떠돌이 길잡이**, then level up until the imported Subclass feature **비전 일격** is granted by stable id.
4. Create a Campaign, open a Session, join P1, resolve the imported feature in Session 1; P1 records it.
5. Kill the Host process and relaunch on the same data root; the module, Background, and grant are still resolvable.
6. Open Session 2, rejoin P1, resolve the imported feature again; P1 records it.

## Iteration log

- Run 1: stopped at Character save (ability-tab choices for the imported Background not completed by the shared helper).
- Run 2: Character saved with the imported Background; one level-up reached level 2, below the Subclass threshold (3).
- Run 3: level-up loop granted the feature; `resolveAction` for the imported feature returned no resolution in the hosted Session — targets/diagnostics added.

## Exact-SHA verification

PENDING.
