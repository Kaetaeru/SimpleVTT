# T2-04 evidence — DM/Player workspace, slices A–C (2026-09-14)

Branch `claude/practical-newton-rye61w`. Screens in `src/table/ui/` over the V2 table facade (`?table=v2`, `localStorage.simplevtt.table="v2"`).
Captured with Playwright/Chromium at 1440×900 against `vite` on Linux, dev preview `?table=v2&session-preview=dm|player` (no network, same runtime for both roles).
Checks at capture: `npm run test:table` 61 + `test:workspace` 3 green · `tsc` · `check-legacy-execution-boundary` · `test:ui-rule-boundary` · `vite build`.

| DM_WORKSPACE.md §8 scene | Covered | Image |
| --- | --- | --- |
| 1 준비실: SRD 체크 → 내 NPC 복제 → 장면 묶음 | 예 (슬라이스 B) — 캠페인 화면 `준비실`: SRD → 내 NPC로, 빈 NPC(공격 한 줄), JSON, PC 프리셋, 장면 묶음 작성; 세션 액터 탭에서 묶음 소환·NPC 드래그 소환·"현재 테이블을 묶음으로 저장" | `prep-1-library.jpg`, `session-1-bundle.jpg`, `session-2-drag.jpg` |
| 2 세션: 묶음 소환 → 이니셔티브 → 토큰 HUD HP −·넘어짐 → 재량 바로 둘에게 중독 → 기록 → Ctrl+Z | 예 (묶음 대신 SRD 다중 소환) | `dm-3-initiative.jpg`, `dm-4-hud.jpg`, `dm-5-log.jpg`, `dm-7-result.jpg`, `dm-9-session.jpg` (되돌린 뒤) |
| 3 자료: 이미지 드롭 → 보기 → 토큰에 드롭 → 공개 | 예 (슬라이스 C) — 호스트 라이브러리의 이미지(여러 장, 1280px 축소)·노트; `보기`는 DM만, `공개`는 모두에게(`handout`), 토큰에 끌어 놓으면 그 플레이어에게만; 노트는 서술로 읽어 줌. 아이템 지급은 토큰 드롭·지급 버튼 둘 다 (`grant-item`) | `session-5-handout.jpg`, `session-6-handout-to-player.jpg`, `player-3-handout.jpg`, `session-3-grant.jpg` |
| 4 플레이어: 손 비우기 → 신성한 불길; 즉흥 행동 → DM DC → 판정 | 부분 — 자유 행동 바(엎드리기·손·물러남·즉흥·귓속말)와 자기 캐릭터 핫바; 주문 시전과 DM 판정 카드는 커널 테스트로만 | `player-1.jpg`, `player-2-attack.jpg` |
| Ctrl+K 통합 검색 | 예 | `dm-8-search.jpg` |

NPC extras (owner ask, 2026-09-14): a library NPC carries a portrait (picked file, shrunk to 384 px), public description, DM-only notes, tags, speed and six ability scores; the portrait sits on the token and in the sheet drawer for everyone, the notes stay on the Host (`prep-2-npc-editor.jpg`, `session-4-portrait-sheet.jpg`).

Slice B (host library, `src/table/library.ts` + `src/table/ui/LibraryPanels.tsx` + `PrepRoom.tsx`): stored as one JSON document in browser storage, campaign-independent; `tests/table/library.test.ts` (3) covers SRD clone with edits, blank NPC, JSON import, presets, favourites/recency across a reload, bundle summon and fold-back, and the `grant-item` command (stacking, weapon → attack tile, characters only).

Slice C: `handout` command/state (one image at a time, to everyone or to one peer; the other peers' wire and log never carry it — `tests/table/handout.test.ts`), `recentCards` (last 20 result cards; players get the public ones) and 기록 → 카드 재열람 (`session-7-reopen-card.jpg`).

What the screens do not yet do: DM read-only PC sheet beyond the summary drawer, Windows H+P1+P2 run.
