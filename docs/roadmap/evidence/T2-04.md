# T2-04 evidence — DM/Player workspace, slice A (2026-09-14)

Branch `claude/practical-newton-rye61w`. Screens in `src/table/ui/` over the V2 table facade (`?table=v2`, `localStorage.simplevtt.table="v2"`).
Captured with Playwright/Chromium at 1440×900 against `vite` on Linux, dev preview `?table=v2&session-preview=dm|player` (no network, same runtime for both roles).
Checks at capture: `npm run test:table` 61 + `test:workspace` 3 green · `tsc` · `check-legacy-execution-boundary` · `test:ui-rule-boundary` · `vite build`.

| DM_WORKSPACE.md §8 scene | Covered | Image |
| --- | --- | --- |
| 1 준비실: SRD 체크 → 내 NPC 복제 → 장면 묶음 | **아니오** — 준비실·NPC 라이브러리·장면 묶음은 다음 슬라이스 (액터 탭의 SRD 다중 선택 소환 ×N만 있음) | `dm-2-summoned.jpg` |
| 2 세션: 묶음 소환 → 이니셔티브 → 토큰 HUD HP −·넘어짐 → 재량 바로 둘에게 중독 → 기록 → Ctrl+Z | 예 (묶음 대신 SRD 다중 소환) | `dm-3-initiative.jpg`, `dm-4-hud.jpg`, `dm-5-log.jpg`, `dm-7-result.jpg`, `dm-9-session.jpg` (되돌린 뒤) |
| 3 자료: 이미지 드롭 → 보기 → 토큰에 드롭 → 공개 | 부분 — 자료 탭은 기존 핸드아웃 패널을 품는다; 토큰 드롭은 없음 | — |
| 4 플레이어: 손 비우기 → 신성한 불길; 즉흥 행동 → DM DC → 판정 | 부분 — 자유 행동 바(엎드리기·손·물러남·즉흥·귓속말)와 자기 캐릭터 핫바; 주문 시전과 DM 판정 카드는 커널 테스트로만 | `player-1.jpg`, `player-2-attack.jpg` |
| Ctrl+K 통합 검색 | 예 | `dm-8-search.jpg` |

What the screens do not yet do (next slice): 준비실 and the host library (NPC/PC 프리셋/장면 묶음), library-item 지급 and 토큰 드롭, drag-to-table, 결과 카드 재열람 from the 기록 tab, DM read-only PC sheet beyond the summary drawer, Windows H+P1+P2 run.
