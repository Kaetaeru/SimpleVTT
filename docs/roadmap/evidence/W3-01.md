# W3-01 Evidence Card

Gate ID: `W3-01`

Classification: `REUSE_LOCKED`

Acceptance criterion: Freeform, ability checks, saving throws, and generic d20 resolution use the existing production authority.

Production entrypoint: the production local-play ActionVm path and existing Common Play/d20 resolver stack. Freeform intent selection exposes real runtime skill actions; those actions delegate d20 arithmetic to the Phase 09 real resolution service, saving throws delegate to the same canonical d20 authority, and portable Common Play d20 definitions lower through the existing generic Resolver semantics.

Existing implementation files:
- `src/playerExperienceModel.ts`
- `src/app/realResolutionService.ts`
- `src/app/realSavingThrowService.ts`
- `src/domain/commonPlayOperationRuntime.ts`
- `src/domain/d20.ts`

Existing automated tests:
- `tests/ui/productionPlayIntentUx.test.ts`
- `tests/ui/productionFreshCharacterSkills.test.ts`
- `tests/ui/phase09RealResolutionService.test.ts`
- `tests/ui/phase09RealSavingThrowAdapter.test.ts`
- `tests/domain/commonPlayD20Runtime.test.ts`

Existing Tauri/Windows evidence: Not required for this `REUSE_LOCKED` Gate. The complete rendered local-session journey is owned by `W3-08`.

Exact observed failure: None. No product-code change is authorized.

Smallest required change: Reuse the existing implementation and record exact-SHA automated evidence only.

## Exact evidence

- Verification SHA: `1a2a5e92f34f3d1dc1a325c9dc6dd39a06eac2ff`
- GitHub Actions UI run/job: `33498144567` / `99824979798` — success
- Gate-owned test cases represented by the five files above: 30 total (5 + 2 + 7 + 8 + 8).
- `productionPlayIntentUx.test.ts` proves the production Scene surface exposes Freeform and maps Influence/Search/Study/Hide to real runtime skill ActionVm entries rather than a parallel intent resolver.
- `productionFreshCharacterSkills.test.ts` proves a freshly created non-fixture Character enters production local Freeform play, resolves proficient and untrained ability checks with queued authoritative d20 faces, records provenance, commits Activity, and does not consume hidden initiative economy.
- `phase09RealResolutionService.test.ts` proves open ability checks project the existing domain d20 result and that the Freeform Athletics adapter delegates d20 arithmetic to the Phase 09 real resolution service.
- `phase09RealSavingThrowAdapter.test.ts` proves saving-throw modifiers come from runtime Character/Combatant stats, save resolution uses the canonical d20 resolver, multi-target results commit atomically, and missing runtime stats reject explicitly without state changes.
- `commonPlayD20Runtime.test.ts` proves portable Common Play d20 definitions lower to the existing generic Resolver, are rename-invariant, cover ability-check/saving-throw/attack-roll families, and reject malformed or missing-authority inputs before commit.
- In UI run `33498144567`, the production UX group, live lifecycle/fresh Character group, Phase 09 real mechanics group, and full `npm run build` all completed successfully. The build includes `commonPlayD20Runtime.test.ts` through the existing Common Play definition/composition suites.

Product-code change: none.
