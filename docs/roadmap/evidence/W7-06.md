# W7-06 — Explicit capability mismatch rejection

Status: PASS
Classification: REUSE_LOCKED
Scenario mapping: MP-A05, MP-G08, MP-G09
Verification head: `196266567ad61506f80d359d60224c6f8be6f186`
Actions checkout: `196266567ad61506f80d359d60224c6f8be6f186` (push to `work/v1-composite`, merge of PR #323)
Run/job: `33873613714` / `101025201957`
Focused tests: 49/49 PASS + production build PASS
Production build: PASS
Artifact: `9937380792`, `W7-06-AUTO-196266567ad61506f80d359d60224c6f8be6f186`, `sha256:08536dc904b27da057346dcce9dbf4bd2080abb01353542e26812b718b7be1ef`

## Reused owners

- `connectedSessionProtocol.test.ts`: the hello handshake rejects protocol-version, rules-profile, and required-capability drift with an explicit reason; Ready Action lifecycle is negotiated as a required capability without bumping protocol v1.
- `productionParticipantLifecycle.test.ts`: an incompatible hello or invalid SessionProjection is rejected before any participant, peer, ledger, or projection state exists.
- `productionSessionLifecycleAdapter.test.ts`: a reference Character cannot enter a production connected Session; Host bind failure returns an actionable offline snapshot.
- `declarativeModuleValidation.test.ts`: RulesProfile, capability, and category incompatibility of content modules are explicit blockers; declared conflicts and dependency cycles are rejected deterministically.
- `commonPlayMaplessTargeting.test.ts` / `commonPlaySpatialFactRuntime.test.ts`: without a spatial fact, distance mechanics are disabled and no visibility/range is invented; with an authoritative fact, range/sight/cover behavior is enforced.
- `phase09RealAtomicAttackAdapter.test.ts` / `productionAcceptanceWindowsRegression.test.ts`: the production attack path uses explicit mapless authority without fabricating spatial facts and enforces range only for an authoritative spatial module fact.
- `mpG09SpatialProviderAuthority.test.ts`: a compatible spatial provider keeps provenance and the Host alone validates dependent mechanics.

## Scenario acceptance map

- **MP-A05** — protocol/rules/content capability mismatch is rejected with an actionable reason and no shared mutation (handshake, participant lifecycle, session lifecycle, module validation owners).
- **MP-G08** — with the optional spatial provider absent, distance/out-of-range mechanics remain disabled and a ranged target is not falsely rejected (mapless targeting, spatial fact runtime, production attack owners).
- **MP-G09** — with a compatible spatial provider present, provider facts carry provenance and the Host alone validates dependent mechanics.

## Exact-SHA verification

`W7-06 AUTO Verification` run `33873613714` on `196266567ad61506f80d359d60224c6f8be6f186` (a real integration-branch commit, the merge of PR #323) passed every focused owner and `npm run build`; the artifact above carries the TAP output, the evidence file, and the ledger snapshot.

No current-HEAD product/runtime or test-implementation defect was reproduced during owner selection. W7-06 is expected to close by reuse plus exact-SHA verification only. The Windows representative capability-mismatch case (MP-A05 WIN) is scheduled under W7-08, per the roadmap.
