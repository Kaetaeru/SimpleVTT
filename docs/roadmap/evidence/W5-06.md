# W5-06 Shared Visual Dice and Combat VFX Evidence

Gate: `W5-06`  
Classification: `REUSE_LOCKED`  
Scope: `local / Host / remote use the same VisualDice and CombatVfx projection path`  
Scenario families: `MP-C`, `MP-I`  
Evidence product SHA: `2ac28651312f1fdbe82edb74fd13f342a8f910f7`  
Canonical evidence-base SHA when this record was prepared: `58a10dc1bf42dff79dd3f8035ea02bd9967d8f43`

## Verdict

The existing production rendering/projection path satisfies W5-06 without a product-code repair. Local play, Host-authored connected presentation, and remote presentation all derive visual dice and combat VFX from the same authoritative `ResolutionView` plus the corresponding action presentation. The remote path does not introduce a network-only dice renderer or VFX resolver.

The strongest focused regression is `tests/ui/connectedResolutionPresentation.test.ts` at the trusted product SHA. Its test `remote presentation metadata reproduces the same shared dice and VFX projections` constructs one authoritative resolution, derives the local action and the remote action reconstructed from the connected presentation envelope, and asserts that both produce identical `buildVisualDiceRoll(...)` and `buildCombatVfxProfile(...)` outputs.

This focused record is ready for official ledger/current reconciliation. Until that later reconciliation commit lands, the authoritative ledger remains unchanged for W5-06.

## Exact-SHA automated evidence

GitHub Actions UI run `33636100212` completed successfully at exact product SHA `2ac28651312f1fdbe82edb74fd13f342a8f910f7` (`conclusion: success`). The canonical W5-04 evidence for the same product SHA records successful connected/session verification, the combat VFX presentation-boundary verification step, and `Typecheck and build` in frontend job `100267245818`.

`tests/ui/connectedResolutionPresentation.test.ts` on that exact SHA includes the focused local/remote equivalence assertion:

- local action: the action from the authoritative local snapshot;
- remote action: `actionFromConnectedPresentation(envelope.action)` from the Host-built connected presentation;
- dice projection: `buildVisualDiceRoll(envelope.resolution, remoteAction)` is deep-equal to `buildVisualDiceRoll(envelope.resolution, localAction)`;
- VFX projection: `buildCombatVfxProfile(envelope.resolution, remoteAction)` is deep-equal to `buildCombatVfxProfile(envelope.resolution, localAction)`.

The same test file also verifies that the remote presentation carries the authoritative dice and that applying presentation on a Client does not mutate mechanics state, so the visual projection is consuming Host-authored results rather than rerunning mechanics.

## Shared production projection owners

### Visual dice

At the trusted product SHA, `src/app/diceVisuals.ts` exports one `buildVisualDiceRoll(resolution, action)` function. It consumes `ResolutionView.authoritativeDice`, derives typed d20/damage/healing presentation and total/modifier metadata, and marks every rendered die `authoritative: true`.

There is no network-specific dice computation in this owner. A connected peer receives the Host presentation's authoritative `ResolutionView` and passes that same shape through the same projection function.

### Combat VFX

At the trusted product SHA, `src/app/combatVisuals.ts` exports one `buildCombatVfxProfile(resolution, action)` function. It derives physical/elemental semantics and delivery/impact phase solely from the supplied authoritative resolution stage and action damage semantics.

The focused connected-presentation regression feeds both the local action and the remote reconstructed presentation action through this same function and requires identical profiles.

## Host-to-remote bridge

W5-04 already fixes the Host presentation boundary on the same trusted product SHA: the Host builds a versioned connected Resolution Presentation Envelope from its authoritative resolution and sends a presentation-only action representation rather than asking Clients to resolve mechanics.

For W5-06, that boundary matters because the remote action is reconstructed with `actionFromConnectedPresentation(...)` only to preserve presentation metadata. The regression then runs the remote representation through the same `buildVisualDiceRoll` and `buildCombatVfxProfile` functions used for the local action. Therefore Host publication and remote consumption preserve one shared rendering projection rather than branching into a network-only renderer.

## Current-tree inheritance check

GitHub compare `2ac28651312f1fdbe82edb74fd13f342a8f910f7...58a10dc1bf42dff79dd3f8035ea02bd9967d8f43` contains no changed files under `src/` or `tests/`. The canonical branch has advanced through evidence/current reconciliation only for this inherited product/test path. The trusted W5-06 implementation and regression therefore remain byte-for-byte inherited from the exact product SHA.

## Acceptance mapping

- local and remote authoritative dice use one projection owner — **PASS evidence** via `buildVisualDiceRoll` and the connected presentation equivalence regression.
- local and remote combat VFX use one projection owner — **PASS evidence** via `buildCombatVfxProfile` and the same equivalence regression.
- Host connected publication preserves the authoritative resolution rather than rerunning mechanics — **PASS evidence** via the already-verified W5-04 Host presentation boundary and presentation-only remote action reconstruction.
- no network-only renderer is required or present for this gate — **supported** by the shared builder functions and the exact regression comparison.
- exact-SHA automation — **PASS evidence** via UI run `33636100212` at `2ac28651312f1fdbe82edb74fd13f342a8f910f7`.
- current canonical inheritance — **PASS evidence** because the trusted product SHA to evidence-base compare has no `src/` or `tests/` changes.

## Reimplementation decision

- Reproduced current-tree product failure: **none**.
- Missing production presentation entrypoint: **none**.
- Local/Host/remote rendering ownership mismatch: **none identified**.
- Product-code change permitted by `REUSE_LOCKED`: **no**.
- This execution changes only `docs/roadmap/evidence/W5-06.md`.
- Next action after this evidence merges: reconcile W5-06 as `PASS` in the official evidence ledger/current documents, then advance to the next canonical unfinished V1 item.
