# W9-01 — Production Windows executable and digest on the exact SHA

Status: PASS
Classification: BUILD
Scenario mapping: V1-70, V1-80
Verification head: `67d40e301558c458802604e6bfdc2f3aa6080bea`
Actions checkout: `67d40e301558c458802604e6bfdc2f3aa6080bea` (push to `work/v1-composite`, merge of PR #328)
Run/job: `V1 Release Artifact` run `33891000536` / `101082346927` (windows-release): PASS
Release executable: `src-tauri/target/release/simplevtt.exe`, 9815552 bytes, `sha256:5d729fa74653853ed380948d00e09e75ea2af249f00ad7ffdb1b479ee3537c0b`
Artifact: `9944155056`, `SimpleVTT-V1-RELEASE-67d40e301558c458802604e6bfdc2f3aa6080bea`, `sha256:f2c0763e143312572f525d264991e996da424647d6461a7793bd830d9697b92a` (contains the executable and `v1-release-evidence.txt`)

## Release path

`.github/workflows/v1-release-artifact.yml` (PR #324) promotes the release build path that already ran inside the W7-04 verification — `cargo test --lib` followed by `npm run tauri:build` — to a dedicated job on every integration push that can change the product, records the executable's SHA-256 and size under the real `V1_VERIFICATION_SHA`, and uploads the executable with its evidence file.

## Notes

`tauri.conf.json` has `bundle.active: false`, so the artifact is the product executable itself (`simplevtt.exe`, product name `SimpleVTT`), not an installer. The gate wording "SimpleVTT.exe" refers to this executable. The final V1 declaration (W9-04) re-runs this job on the final integration SHA so that the declared SHA and the shipped digest match.
