# T2-07 evidence — switch-over, first half (2026-09-14)

Branch `claude/practical-newton-rye61w`. The V2 table runtime (kernel `src/table/` + DM workspace `src/table/ui/`) is now the product session path.

## What changed

- `src/main.tsx` composes `createTableSessionFacade(mockAdapter)` unconditionally. The old play screens are an explicit opt-out for the transition: `?table=legacy` or `localStorage.simplevtt.table = "legacy"`. There is no `?table=v2` any more.
- `ProductRoot` (live session) and `SessionDebugPreview` (dev preview) render `TableWorkspace` whenever the facade is present — unchanged code, now the default.
- The facade's live session takes its name from the prepared campaign session (`campaignSessionSnapshot.sessionName`) instead of the mock's reference name.
- `tests/table/switchover.test.ts` (2) guards the composition: facade by default, `legacy` opt-out only, workspace on both surfaces.
- `tests/table/scenario3.test.ts` (2): scenario 3 "DM 재량" offline on the kernel — natural 1 (auto miss) → DM narrates and drops the sword (DM `object drop` on a PC), draw a dagger (free interaction) and the second interaction refused, next turn pick up, improvised 모래 뿌리기 ruled from the card's suggestion (blinded), one `ruling` on three goblins (공포), undo of just that ruling; replay parity and no unspent dice. The second test runs the mass ruling and undo through the facade (the product path).

## Captures (`t2-07/`, Chromium 1440×900 against `vite`, Linux, no flag)

| Step | Image |
| --- | --- |
| 캠페인 → 세션 시작 (설정) | `switch-1-campaign-session-setup.jpg` |
| 준비 화면으로 → 세션 시작 화면 (기존 화면, 캠페인에서 세션 만들기) | `switch-2-preparation.jpg` |
| 세션 열기 → DM 워크스페이스 (플래그 없음, 솔로 테이블) | `switch-3-workspace-default.jpg` |
| `?table=legacy&session-preview=dm` → 기존 플레이 화면 | `switch-4-legacy-opt-out.jpg` |

Script result: `facadeOnWindow: true`, `workspaceAfterHost: 1`, `legacyWorkspace: 0`, `legacyHasOldScreen: true`, no page errors.

## Checks at this head

`npm run test:table` (kernel + scenario 3 + switch-over) and `test:workspace`, `tsc`, `check-legacy-execution-boundary`, `test:ui-rule-boundary`, `vite build`, plus the ten `tests/ui` structure tests that read `main.tsx` — all green on Linux.

## Not done (the gate's second half)

- **Old session adapters are still composed** in `main.tsx`. Creation, sheet, level-up, campaign systems, party stash and the 세션 시작 screen still run through them (the facade proxies everything it does not override). Deleting them removes ~150 `tests/ui` files and the Windows runner scripts that drive the old screens; that is an owner decision after the Windows run, not a unilateral step. The legacy execution boundary is therefore unchanged.
- **Windows H+P1+P2** run of scenario 3 on the new runtime; PR into `work/v1-composite`; release artifact.
