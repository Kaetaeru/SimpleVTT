# W2-01 Evidence Card

Gate ID: `W2-01`

Classification: `REUSE_LOCKED`

Acceptance criterion: lock the shipped SRD 5.2.1 Level-1 character-creation catalog at 12 classes, 9 species, 4 backgrounds, with every shipped class able to reach a legal Level-1 commit.

Production entrypoint: existing Character V1.0 data/adapter path backed by `content/indexes/dnd-srd-5.2.1.character-creation.json` and `src/app/characterCreationV10Data.ts`.

Existing implementation files:
- `content/indexes/dnd-srd-5.2.1.character-creation.json`
- `src/app/characterCreationV10Data.ts`
- `src/app/characterCreationV10Adapter.ts`

Existing automated tests:
- `tests/ui/characterCreationCompleteGate.test.ts`

Existing Tauri/Windows evidence: Not required for this `REUSE_LOCKED` catalog gate. Rendered lifecycle verification belongs to `W2-08`.

Exact observed failure: None. No product-code change is authorized.

Smallest required change: No product-code change. Reconcile the already-passing exact-SHA catalog evidence into the V1 ledger with provenance.

## Exact evidence

- Canonical passing verification SHA: `42305b1d2a66a976b08844509a63b5999166938a`
- GitHub Actions UI run: `33387465586` — success
- Focused test: `tests/ui/characterCreationCompleteGate.test.ts`
- The test explicitly asserts `CLASSES.length === 12`, `SPECIES.length === 9`, and `BACKGROUNDS.length === 4`.
- The same test iterates all 12 shipped SRD classes and completes each through the existing Guided Create path to a saved Level-1 character with zero blocking/warning validation.
- Current integration HEAD reviewed for inheritance: `c6257b6637603cd35abc7f6c79ab706b36aaa7fb`.
- `42305b1d2a66a976b08844509a63b5999166938a..c6257b6637603cd35abc7f6c79ab706b36aaa7fb` is 30 commits ahead with no changes to `src/app/characterCreationV10Data.ts`, `content/indexes/dnd-srd-5.2.1.character-creation.json`, or `tests/ui/characterCreationCompleteGate.test.ts`.
- Later changes touched the adapter/library/Tauri harness for W1 lifecycle work, not the catalog definitions or this focused catalog test.

Under `docs/roadmap/EVIDENCE_CARD.md`, older exact-SHA evidence may be inherited when the relevant implementation path is unchanged and provenance is recorded. This card records that inheritance explicitly.

Product-code change: none.
