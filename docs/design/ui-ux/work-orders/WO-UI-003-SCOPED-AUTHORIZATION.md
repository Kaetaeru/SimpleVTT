# WO-UI-003 — Scoped Runtime Authorization

Status: **RECORDED / ACTIVE**

Date: 2026-08-22

Work Order:

`WO-UI-003-connected-play-actor-boards-command-center.md`

---

# Owner authorization

Immediately after confirming that the current Connected Play runtime layout does not match the Owner-accepted integrated reference, the assistant described the required next runtime work as:

- Upper opposing Actor Board;
- broad mapless Tabletop Stage / Play Context;
- Lower allied Actor Board;
- persistent bottom Command Center;
- reconciliation of the current historical intent-first action funnel with the accepted direct Hotbar/capability contract;
- preservation of current Session/runtime authority and exclusion of unresolved Main Hand/locking/privacy/network gaps.

The owner explicitly authorized proceeding:

> 그래 가자

This is recorded as explicit implementation authorization for the bounded WO-UI-003 slice only.

---

# Authorized dependencies

For this bounded slice, implementation may rely on the reviewed Product/UX decisions named in the Work Order, including the persistent dual-anchor Play model, Actor Board overflow behavior, compact Initiative extension, and BG3-family Command Center composition.

This is not a global freeze or blanket authorization for unrelated UI/Domain/Architecture work.

---

# Explicit non-authorization

This record does not authorize:

- Core battlemap/grid/token/coordinate features;
- new rules calculations in UI;
- Main Hand default hostile-click behavior while its canonical relation Gap remains unresolved;
- selective resolution-safe conflict calculation;
- private/DM-only delivery protocol changes;
- Handout networking architecture;
- Session transport rewrite;
- Character rules/progression changes;
- invented neutral relation or Hotbar persistence schemas;
- automatic implementation of later Work Orders.

---

# Acceptance boundary

Automated verification is necessary but not sufficient.

WO-UI-003 remains open until the owner visually exercises the Tauri Connected Play runtime and confirms that the accepted Actor Boards / Tabletop Stage / Command Center direction is correctly represented.
