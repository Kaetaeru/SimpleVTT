# 새 클라이언트 — 전면 재작성 계획 (V3)

- 상태: **착수 (2026-09-14)**. 소유자 지시: "모든 부분을 전면적으로 새로 만든다는 느낌으로. 멀티세션은 나중으로 미루고, 캐릭터 시트를 완벽하게 생성 가능하게. JSON도."
- 캐릭터 모델은 [캐릭터 시스템 재설계](../v2/CHARACTER_SYSTEM.md) §4(원본 · 계산 · 사용량)를 그대로 쓴다. 이 문서는 그 위에 "무엇으로 어떻게 구동하고, 어떤 순서로 만드는가"를 정한다.
- 기존 `src/`(옛 앱, V2 테이블 포함)는 손대지 않고 그대로 둔다. 새 클라이언트가 같은 기능을 덮을 때마다 옛 화면을 퇴역시킨다.

## 1. 구동 방식 — 결정

**웹 우선 + Tauri 셸.**

- 새 클라이언트는 표준 웹 앱(Vite + React 19 + TypeScript)이고 **브라우저만으로 전 기능이 돈다**: 저장은 IndexedDB, 캐릭터·모듈은 JSON 파일로 내보내기/가져오기. 개발·검사는 `npm run dev:client`로 브라우저에서 한다. 소유자 기기에 Rust가 없어도 된다.
- 배포 exe는 기존 Tauri v2 셸이 그대로 감싼다(CI의 `windows-exe` 워크플로가 이미 exe를 만든다). Tauri는 나중에 네이티브 파일 저장(데이터 폴더)과 멀티세션 네트워크(기존 Rust TCP 또는 WebSocket, M4에서 결정)를 더한다.
- 검토한 대안: Electron(150MB+, M1에 필요한 Node API가 없어 이점이 없음), PWA 전용(멀티세션 호스트를 열 수 없음). Tauri는 10MB exe와 CI 파이프라인이 이미 있다.

## 2. 저장소 배치

```text
client/                     새 클라이언트 (옛 src/를 import하지 않는다 — 경계 검사로 강제)
  index.html · main.tsx
  app/                      셸·라우트·전역 상태
  catalog/                  ContentCatalog: content/modules(SRD) + 설치 모듈(JSON) → 하나의 카탈로그, 긴 id, 한글 이름
  rules/                    SRD 5.2.1 표: 숙련 보너스, 포인트 바이, 멀티클래스 슬롯, 계약 마법, 갑옷 공식, 종족·서브클래스 표
  character/                CharacterSource · CharacterRuntime · deriveCharacter(부여 원장 · 필요 선택 · 파생값 · 검증)
  storage/                  IndexedDB 저장소, JSON 내보내기/가져오기, 모듈 설치
  screens/                  라이브러리 · 생성 마법사 · 시트 · 가방 · 레벨업 · 콘텐츠
  ui/                       디자인 토큰·기본 컴포넌트 (DM 워크스페이스의 13px·32px·두 단계 표면)
  data/                     저장소 안에 없던 SRD 데이터 (서브클래스 표, 종족 선택 표, 특성·재주 설명)
tests/client/               node --test + tsx (옛 테스트와 분리)
```

- 읽기만 하는 데이터: `content/modules/**`, `content/indexes/**`, `src/generated/*.json`. 코드는 가져오지 않는다.
- 스크립트: `dev:client`, `build:client`(→ `dist-client/`), `test:client`, `check:client-boundary`. Tauri는 `src-tauri/tauri.client.conf.json`으로 `dist-client`를 가리킨다.

## 3. 이정표

| 이정표 | 내용 | 완료 기준 |
| --- | --- | --- |
| **M1 캐릭터 생성** | 카탈로그 + 계산 엔진 + 저장/JSON + 생성 마법사 + 시트(읽기) + 콘텐츠 설치 | §4 수락 목록 전부, 전수 검사 green, exe |
| M2 시트 운용 | 사용량 인라인, 가방·장착·손·조율, 개인 휴식, 레벨업, 원본 편집, 종이형 시트 | CHARACTER_SYSTEM §3.3·§5.2~5.4 |
| M4 멀티세션 (M3보다 먼저, 2026-09-15 소유자 결정) | 캠페인·리소스 모델 + 호스트 권위 세션 + LAN/하마치 TCP + 파티 플레이 + 자산·핸드아웃 + 장면·토큰 — 설계는 `CAMPAIGN_RESOURCES.md` §9 S1–S7 | S1–S7 각 검사 + 두 클라이언트 E2E |
| M3 테이블 | V2 테이블 커널을 장면 문서 위에 이식 | 시나리오 1·3 |
| M5 준비·캠페인·퇴역 | 준비실·캠페인·자료, 옛 `src/` 제거 | 전체 게이트 |

## 4. M1 수락 목록 — "완벽한 생성"

CHARACTER_SYSTEM §3.1의 모든 행에 더해:

1. **모든 SRD 조합**: 종족 9(선택 있는 종족의 혈통·유산·조상·기술·재주 포함) × 배경 4(능력치 +2/+1·+1/+1/+1, 기원 재주, 도구, 장비) × 직업 12(1레벨 선택 전부: 기술, 무기 통달, 전투 방식, 신성한/원초적 역할, 악기, 도구, 전문화, 언어, 기원술, 소마법·준비 주문·주문서) — 빠지는 선택이 없다.
2. **원하는 시작 레벨(1~20)**: 레벨 기록을 순서대로 채우고 서브클래스(3레벨)·ASI/재주(일반 재주만, 선행 조건)·주문 증가·교체·통달 수·전문화·기원술·초월 마법·서사시 은혜(19)까지 묻는다. 멀티클래스(선행 조건, 1레벨 숙련만, 합산 슬롯, 계약 마법 별도).
3. **파생값 전부**: 최대 HP(고정/굴림, 건강, 강인함·드워프), AC(갑옷·방패·비무장 방어·마법사 갑옷), 속도·감각·크기, 숙련 보너스, 내성·기술(전문화)·패시브 지각·이니셔티브, 주문 DC·명중·슬롯·소마법/준비/주문서/항상 준비, 공격(무기별·통달), 자원 풀, 저항·면역, 언어·도구, 특성·재주·배경 목록 **설명 포함**.
4. **설명**: 직업 특성 191 + 서브클래스 특성, 종족 특성, 재주, 배경, 주문 339 모두 한글 설명이 붙는다. 저장소에 없던 설명은 SRD(CC-BY-4.0) 요약으로 우선 채우고 `descriptionSource: "srd-summary"`로 표시한다. 소유자 번역본이 오면 같은 id로 교체한다.
5. **JSON**: (a) 캐릭터 내보내기/가져오기(원본 + 사용량, 스키마 v2, 검증과 오류 보고), (b) 콘텐츠 모듈 설치 — 기존 RuleModule JSON(SRD 모듈과 PHB 보충 모듈이 쓰는 형식) 그대로. 설치한 종족·배경·재주·서브클래스·주문이 생성·시트에서 내장과 똑같이 흐른다. 기원 재주는 ASI 후보에 나오지 않는다.
6. **검증**: 능력치 상한·포인트 바이 합, 선행 조건, 중복 선택, 알 수 없는 id, 갑옷 숙련 경고. 막힘이 있으면 저장하지 않는다.
7. **전수 검사**: 종족 9 × 직업 12 × 배경 4 × 레벨 1→20을 결정적 기본 선택으로 만들어 골든 표와 비교한다. 화면 캡처는 대표 6조합.

### 4.1 M1 진행 상태

| 조각 | 내용 | 상태 |
| --- | --- | --- |
| C1 카탈로그 | `client/catalog/` — SRD 모듈 36개 + 생성 색인 + 진행표 + 주문 339 + 저작 데이터(`client/data/srd/`)를 한 id 공간으로. 경고 0 | 완료 |
| C1 엔진 | `client/character/` — `deriveCharacter(source, catalog)`: 원장(ledger) 한 번 순회로 종족·배경·트랙·장비를 적용하고 선택 요청(`ChoiceRequest`)을 등록, 끝에서 숫자를 계산. 자동 채우기(`autofill`)로 검사·빠른 생성 | 완료 |
| C1 검사 | `tests/client/derive.test.ts`(17 시나리오) + `tests/client/matrix.test.ts`(종족 9 × 직업 12 × 배경 4 × L1·L5 = 864, 직업 12 × L1~20 = 240, 독립 공식 대조) | green |
| C2 저장·JSON | `client/storage/store.ts`(IndexedDB `simplevtt-client` v1: characters/modules/settings, 메모리 폴백), `client/character/json.ts`(파일 형식 `simplevtt.character` 스키마 2 = source + runtime + summary, 필드별 검증·오류 일괄 보고, 미설치 모듈 id 목록), `client/catalog/install.ts`(RuleModule JSON 파싱·요약·의존성). 검사: `install.test.ts`(합성 보충 모듈 → 종족·배경·기원 재주·일반 재주·서브클래스·주문이 생성까지 흐름, 기원 재주는 ASI 후보 밖), `json.test.ts`, `storage.test.ts`(fake-indexeddb) | 완료 |
| C3 화면 | `client/app/`(셸·해시 라우터·컨텍스트), `client/screens/`(라이브러리, 생성 마법사 7단계 + 오른쪽 실시간 시트·검증, 시트, 콘텐츠 설치 미리보기, 선택 피커), `client/ui/`(토큰 사본·스타일). `npm run dev:client`(포트 1430) · `npm run build:client` · `npm run tauri:build:client`(`src-tauri/tauri.client.conf.json`, dist-client). 워크플로 `windows-exe-client.yml`이 `client-m1-*` 프리릴리스로 exe를 올린다. 검사: `screens.test.ts`(정적 렌더) + `npm run capture:client`(Chromium이 마법사→시트→레벨업→JSON→모듈 설치→설치 콘텐츠 캐릭터까지 진행, `docs/evidence/new-client-m1/` 15장) | 완료 |

화면 규약: 마법사는 원본만 바꾸고(`setChoice`/`addLevel`…) 매 변경마다 `deriveCharacter`를 다시 돌려 오른쪽 시트와 검증을 갱신한다. 막힘(blocking)이 하나라도 있으면 저장 버튼이 잠긴다(수락 6). 새 캐릭터 초안은 설정 키 `creation-draft`에 자동 저장되어 창을 닫아도 남는다. "남은 선택 빠르게 채우기"는 `autofill`이다. 시트의 현재 HP만 런타임을 바꾼다(나머지 운용은 M2).

**M2 착수 (2026-09-15, 소유자 지시 "숫자 출처 추적 + 오프라인 세션처럼 시트 조작")**: 파생값마다 `terms`(가산 항목)를 붙여 시트에서 마우스를 올리면 출처가 나온다(HP·AC·이니셔티브·속도·패시브 지각·능력치·내성·기술·공격 명중/피해·주문 DC/명중). 시트는 런타임을 직접 조작한다: HP 슬라이더와 HP 입력 한 칸(`12` 현재 HP 설정 · `-4` 피해 · `+4` 회복 · `++4` 임시 HP, `applyHpCommand`), 히트 다이스 소비, 짧은 휴식(자원 회복 규칙 `restore.short`, 계약 슬롯)·긴 휴식(전부 회복, 히트 다이스 절반, 탈진 −1), 슬롯·자원 핍 클릭, 금화 증감, 아이템 추가(카탈로그 검색 또는 직접 입력)·수량·버리기·착용(AC·공격에 반영), 상태 토글, 탈진, 죽음 내성, 영웅적 영감, 활동 기록 200줄. 연산은 `client/character/play.ts`의 순수 함수이고 `play.test.ts`가 덮는다. 런타임 스키마에 `inventory`(제거·수량·추가)와 `log`가 추가됐다(JSON v2 호환, 없으면 빈 값).

**주사위·레벨 업 (2026-09-15)**: 옛 클라이언트의 물리 주사위(`PhysicsDice3D`, three.js + cannon-es)를 `client/ui/dice/`로 복사해 `DiceProvider`가 오버레이로 굴린다(`useDice().roll({label, formula})` → 결과 Promise). 시트의 기술·능력치 판정·내성·이니셔티브·공격 명중/피해와 임의 식(`2d6+3`), 짧은 휴식 히트 다이스, 마법사와 레벨 업 화면의 HP 굴림이 모두 이 주사위를 쓰고 기록에 남는다. 1레벨 HP는 규칙대로 최대값(굴리지 않음)이며 마법사에 그렇게 표시한다. 레벨 업은 생성 마법사와 별개 화면(`#/levelup/<id>`): 직업과 올릴 레벨 수 → 레벨별 HP 고정/굴림 → `choicesOpenedByLevelUp`이 고른 "이번 레벨 업이 새로 연 선택"만 → 바뀌는 것 요약(HP·숙련 보너스·새 특성) → 적용. 검사: `dice.test.ts`(식 파싱·RNG 주입·레벨 업 선택 필터). 주사위 장면은 `PhysicsDice3D`의 `dice` 배열을 `useMemo`로 고정해 렌더마다 씬을 다시 만들지 않는다(안 그러면 굴림이 보이지 않았다).

**특성 사용·주문 시전 (2026-09-15, 소유자 지시 "사용 버튼, 턴 기반이면 종료 버튼")**: 시트의 특성마다 `client/rules/activation.ts`의 규칙으로 "사용" 버튼이 붙는다 — 표(격노·재기의 바람·행동 폭증·불굴·바드의 영감·신성 변환·야생 변신·기 점수·안수·강타·주적·지치지 않음·자연의 장막·선천 마법 등), 없으면 특성 이름과 같은 자원 풀(종족 브레스 무기 등), 그것도 없으면 설명이 "행동으로/추가 행동으로"인 특성은 기록만 남기는 사용. 사용은 풀을 차감하고(안수는 점수 입력), 회복·임시 HP 식이 있으면 주사위 오버레이로 굴려 적용하며(재기의 바람 1d10+레벨), 지속 시간이 있으면 런타임 `effects`에 효과를 올린다. 주문은 소마법·항상 준비·준비 주문마다 행으로 나오고 "시전"은 낼 수 있는 값(주문 레벨 이상 남은 슬롯, 계약 슬롯, 무료 시전 풀, 의식)을 골라 차감한다. 지속 시간은 카탈로그 문자열을 `parseDuration`이 해석한다: "즉시/순간"은 효과 없음, "집중"은 새 집중이 이전 집중을 자동 종료, 1라운드·1분·10분은 라운드 수(≤100)로 세고 시간 단위는 글자만. 진행 중인 효과 카드에서 "종료"로 끝내거나 "다음 라운드"로 라운드를 세어 다 되면 저절로 끝난다(격노 10라운드). 짧은 휴식은 집중·라운드 효과를, 긴 휴식은 모든 효과를 끝낸다. 검사: `play.test.ts`(격노 10라운드·재기의 바람·안수 점수·시전 슬롯/의식/소마법/집중 교체/휴식). 증거 24–26.

**효과가 숫자에 닿는다 (2026-09-15, 소유자 지시 "격노 피해 보너스가 안 들어갔다 — 온오프 기술 내용을 다 적용")**: `client/rules/effects.ts`가 진행 중인 효과마다 규칙을 갖고, `deriveCharacter(source, catalog, { effects })`가 파생 뒤에 이를 적용한다(`applyActiveEffects`). 바뀐 값은 모두 효과 이름을 단 `Term`으로 붙어 마우스를 올리면 보인다. 규칙 예: 격노(근력 근접·투척 공격 피해 +2/+3/+4, 타격·관통·참격 저항, 메모), 선천 마법(주문 DC +1), 사냥꾼의 표식/헥스(무기 피해 +1d6), 축복(명중·내성 +1d4), 방패(+5 AC), 신앙의 방패(+2), 마법 갑옷(비무장이고 더 높을 때 13+민첩), 나무 껍질(최소 17), 가속(+2 AC, 속도 ×2), 장거리 보행(+10), 비행/거미 등반, 지원(최대 HP +5, 시작 시 현재 HP +5), 인도/저항(판정·내성 +1d4), 확대(무기 피해 +1d4), 신성한 은총, 마법 무기/원소 무기, 흔적 없이 지나가기(은신 +10), 돌 피부, 영웅심(공포 면역), 수호의 유대, 암시야, 그리고 투명·잔상·흐림·성역·죽음 방비 등은 메모만. 주사위 보너스는 `Term.dice`("1d4")로 실려 시트의 굴림 식에 `+1d4`로 붙고 Explain에 표시된다. 규칙이 없는 효과는 카드에 "수동"으로 표시된다. 저장 시 `saveCharacter`도 런타임의 장비·가방·효과로 파생해 reconcile하므로 지원의 +5가 깎이지 않는다. 검사: `effects.test.ts`. 증거 27.

**M4 (Roll20 모델) R1 캠페인 홈 + R2 채팅 (2026-09-15)**: 1차 세션·캠페인 구현을 되돌린 뒤 `ROLL20_MODEL.md`대로 다시 만들었다. `client/campaign/model.ts`(캠페인 = 유일 단위, 참가자 목록·역할·색·내보내기, 게임당 고정 참가 코드 `joinCode`, 설정, 채팅 보관함 문서), `client/session/`(프로토콜 v2: hello에 참가 코드, presence·chat·kicked 이벤트 / `chat.ts`: `/roll` `/gmroll` `/w` `/em` `/desc` `[[…]]` 파서와 Roll20 가시성(귓속말은 보낸 사람·대상·GM, GM 굴림은 굴린 사람·GM) / `TableHost`: 코드 검사, 내보낸 참가자 거절, 참가자 기억, 채팅을 보관함 메시지로, GM만 역할 변경·내보내기, 뷰어별 투영 리플레이 / `TableClient` / 전송: 메모리·BroadcastChannel·Tauri TCP 복원, Rust `list_session_addresses` 복원), `app/campaigns.tsx`(내 캠페인·참가한 캠페인·보관함, 게임 시작 = 호스트 기동(탭 채널 + exe면 TCP와 `IP:41230-코드` 초대), 코드로 입장, 보관함 500ms 배치 저장), 화면: 캠페인(두 목록·새 캠페인·코드 입장), 캠페인 상세(게임 시작/테이블로, 코드 복사·다시 만들기, 참가자 GM 승격·내보내기·다시 허용, 설정 3개·소개, 보관함 개수, 삭제), 테이블(참가자 줄, 코드, 페이지 자리(R5), 사이드바 채팅 탭: 주사위 카드·귓속말·GM 굴림·서술, 굴림은 3D 주사위를 거쳐 전송). 검사: `table.test.ts` 5건, 두 탭 E2E `capture-client-table.mjs` SC-1~8(증거 28–35).

**M4 R3 저널 (2026-09-15)**: `client/campaign/journal.ts`(핸드아웃·캐릭터 항목, 폴더, 볼 수 있는 사람/고칠 수 있는 사람 = 없음·모든 플레이어·개별, GM 노트, 보관, 태그, 뷰어별 투영 `projectEntry`, 플레이어 편집 병합 `mergePlayerEdit`, 폴더 트리, 본문 서식 파서(# 제목·- 항목·**굵게**·링크·`@[항목]` 저널 링크 D85)), 프로토콜 v3(`journal.put/remove/show` 명령, `journal`/`journal.removed`/`journal.show`/`settings` 이벤트; 호스트가 모든 이벤트를 보낼 때·리플레이할 때 뷰어별로 투영 — 권한 밖이면 제거 이벤트로), 호스트 규칙(GM은 전부, 플레이어는 설정이 허용하면 캐릭터만 새로 만들고 자기 것만 고침, 핸드아웃·삭제·보여주기는 GM만), 저장은 항목마다 문서 한 줄(`onJournal`), 캠페인 삭제 시 함께 삭제. 화면: 테이블 사이드바 저널 탭(검색, 폴더 접기, + 핸드아웃, + 캐릭터, 라이브러리에서 가져오기, 보관함 보기), 떠 있는 창(끌어서 이동, 여러 개, 앞으로 가져오기), 핸드아웃 창(아바타·이름·본문 편집/보기·GM 노트·권한·폴더·태그·보여주기·보관·삭제), 캐릭터 창 3탭(정보: 소개·편집 마법사·라이브러리로 내보내기·복제 / 시트: `SheetPlay` 운용 패널 + 시트, 굴림이 채팅 카드로 / 속성: 파생값 표), 새 캐릭터 창(마법사 `CreateScreen` embedded 모드), "플레이어에게 보여주기"가 볼 수 있는 사람의 창을 자동으로 연다. `SheetScreen`은 `SheetPlay`의 얇은 래퍼가 됐고 저장 규칙은 `character/save.ts:resolveRuntime`으로 공유. 검사: `journal.test.ts` 4건(66 tests), 두 탭 E2E `capture-client-journal.mjs` SC-13~19(증거 36–42).

**M4 R4 아트 라이브러리 (2026-09-15)**: `client/campaign/art.ts`(아트 문서: 이름·폴더·mime·크기·치수·내용 해시·128px 썸네일·소유자·태그; `artVisible` = 소유자·GM·보이는 저널 항목이 아바타로 참조; `chunkText`·`ChunkAssembler`·`hashText`), 저장소 v3 `assets`(해시 → data URL; 호스트의 파일이자 모든 뷰어의 캐시), 프로토콜 v4(`art.upload`+`art.chunk` 업로드, `art.update/remove`, `art.fetch` → 그 피어에게만 `art.data` 조각; `art`/`art.removed` 이벤트를 뷰어별 투영 — 참조를 잃으면 제거로), 호스트(소유자 도장, 20MB 제한, 저널 저장 시 참조된 아트 재전송), `TableClient.fetchArt`(진행률), 프로바이더(`uploadArt` = 읽기·치수/썸네일·해시·캐시·조각 전송, `requestArt` = 캐시 → 호스트, `useArtUrl` 훅, "자료 받는 중 n"), 화면 `ArtPanel.tsx`(아트 탭: 검색·폴더·올리기·끌어 놓기·붙여넣기·카드·상세(이름·폴더·태그·참조 복사·삭제)·올린 사람 배지, `ArtPicker` 모달, `ArtDropZone`, `ArtImage` = 썸네일 먼저·원본 도착 시 교체), 저널 아바타가 `art:<id>` 참조(라이브러리에서 고르기·올리기·끌어 놓기). 검사: `art.test.ts` 3건(70 tests), 두 탭 E2E `capture-client-art.mjs` SC-22~25(증거 43–46).

**M4 R5 페이지·토큰 (2026-09-15)**: `client/campaign/page.ts`(페이지: 칸 단위 크기·축척·격자·배경·안개 플래그·보관·토큰 목록 / 토큰: 레이어·위치·크기·회전·이미지·represents·controlledBy(상속)·이름표·바 3개(값·최대·연결·보임·편집)·바 스타일·오라 2개·틴트·마커(뱃지)·GM 노트·잠금·뒤집기·z·시야·빛; 마커 세트 = 5e 상태 14종 + 집중·사망·은신·엄폐 + 색 점; `tokenForCharacter`(기본 토큰), `controlsToken`, `playerPageId`(리본·북마크), `projectPage/Token`(플레이어는 자기 페이지만, GM 레이어·GM 노트·숨긴 바/오라 제거), `mergeControllerTokenEdit`(위치·회전·뒤집기·마커·편집 가능한 바만), 격자 맞춤·거리·바 입력 문법), 캠페인에 `playerPageId`·`pageBookmarks`, 프로토콜 v5(`page.put/remove/ribbon/bookmark`, `token.put/remove`, `ping`; `page`/`page.removed`/`ribbon`/`token`/`token.removed`/`ping` 이벤트를 뷰어별 투영, 리본이 움직이면 페이지 전부 재전송), 호스트(GM 규칙, 플레이어는 자기 페이지에 자기 캐릭터 토큰만 놓고 자기 토큰만 움직임, 연결된 바를 시트에서 채움 `attributeOf`, 편집 가능한 hp 바 수정 → 시트 HP + 채팅 기록, 캐릭터가 바뀌면 그 토큰들 갱신), 프로바이더(페이지 문서 저장·삭제, 명령, 핑 목록 2.5초), 화면 `PageCanvas.tsx`(페이지 줄·리본 옮기기·추가/복제/설정/보관/보관함/파티 나누기, 왼쪽 도구막대 레이어·확대 10 단계·Ctrl+휠, 격자 캔버스, 토큰: 이미지/이니셜·이름표·바·마커·오라·틴트·잠금·선택 테두리·드래그(맞춤·Alt·거리 표시)·화살표·Delete·Shift+클릭 핑·우클릭 메뉴(바 `-5/+3/12`·마커 격자·시트 열기·토큰 설정·레이어·앞/뒤·기본 토큰 저장·복제·잠금·삭제), 저널/아트 끌어 놓기, 놓은 토큰으로 스크롤), 토큰 설정 창(기본/고급), 페이지 설정 창(배경 이미지는 아트 라이브러리), 저널 행 끌기와 "토큰" 버튼. 검사: `pages.test.ts` 3건(73 tests), 두 탭 E2E `capture-client-pages.mjs` SC-27~32(증거 47–52).

**M4 R6 턴 트래커·컴펜디움·대상 지정 (2026-09-15)**: `client/compendium/monsters.ts`(생성된 SRD 괴물 329종을 `MonsterView`로: 공격 보너스·피해 주사위, 내성 행동 DC·범위, 재충전 타이밍, 전설 행동, 주문 시전; 검색·CR/유형 필터·몸집 → 칸), 저널 세 번째 종류 `JournalNpc`(스탯 블록 사본 + `NpcRuntime`: HP·상태·전설 사용·재충전 대기), `tokenForNpc`(비연결 바 = 토큰마다 HP, D78), `client/campaign/tracker.ts`(행·현재·라운드·정렬, 토큰별 upsert, 커스텀 행 `+1` 공식, `advanceTurn`이 한 바퀴 돌면 라운드 +1), 캠페인에 `tracker`, 프로토콜 v6(`tracker.set/add/next`, `tracker` 이벤트; `tracker.add`에 `rollBonus`를 주면 호스트가 1d20을 굴려 채팅 카드로), 호스트 `nextTurn`(턴 끝: 캐릭터 `advanceRound` / 턴 시작: NPC 재충전 굴림·전설 초기화, 0 HP PC 죽음 내성 자동(20이면 HP 1, 1이면 실패 2회) / "○○의 턴"·"라운드 N"), 토큰 삭제 시 행 제거, 화면: `TrackerWindow`(GM이 열면 모든 참가자 창에, ▶ 다음 턴·전투 시작·정렬·비우기·▲▼·삭제·커스텀 행·이니셔티브 편집, 플레이어 읽기 전용), `CompendiumPanel`(괴물/주문/아이템 탭, 검색·필터, 스탯 블록 펼치기, "놓기"·끌어 놓기 → NPC + 토큰), `NpcSheet`(스탯 블록: 능력치 판정/내성, 행동마다 명중·피해, 내성 행동 DC, 재충전 대기 표시, 전설 행동 카운터, HP `-5/+3/12/++4`, 상태, GM 필드), `PageCanvas`: 대상 지정 모드(`requestTargets` 브리지, 과녁 배너, 단일/다중 Shift, 확정/Esc, 고른 토큰 점선), 토큰 액션 줄(D88: 이니셔티브 굴림 → 트래커, PC 공격 명중/피해, NPC 공격·내성 행동·재충전 소비, 시트), 현재 턴 토큰 테두리, 우클릭 "턴 트래커에 추가"(0), 컴펜디움 끌어 놓기, 테이블 헤더 "턴 트래커" 토글. 검사: `tracker.test.ts` 3건(76 tests), 두 탭 E2E `capture-client-tracker.mjs` SC-34~38(증거 53–58).

**M4 R7 공격·피해 판정 (2026-09-15)**: Roll20과 갈라지는 지점 — 주사위만 굴려 주는 게 아니라 앱이 규칙을 계속 추적한다. `client/rules/resolve.ts`(`Combatant`·`AttackSpec`·`resolveAttack`: 상태·효과·사거리에서 유리/불리 제안(`suggestAdvantage`), d20 둘 다 보관, 엄폐 +2/+5, 명중 합계 vs AC, 20 치명/1 자동 실패, 마비·무의식 대상 5 ft 안 자동 치명, 치명 주사위 두 배, 피해 유형별 저항/면역/취약(한·영 이름), 임시 HP 먼저 흡수, 집중 중이면 CON 내성 자동 DC max(10, 피해/2)·실패 시 효과 종료, 0 HP → PC 무의식·넘어짐·죽음 내성 시작 / NPC 사망 / 남은 피해 ≥ 최대 HP 즉사, 명중 시 조건 부여; `fixed`로 같은 주사위 재판정), `client/rules/attackSpec.ts`(시트 공격 행 → 스펙: 사거리·장거리, 암습 Nd6(민첩/원거리), 신성한 강타 (1+슬롯)d8 광휘 최대 5d8 + 슬롯 소비; NPC 행동 → 스펙; 토큰의 비연결 바 = NPC HP), 프로토콜 v7(`act.attack/adjust/undo/confirm`, `ActorRef`·`AttackRef`·`AttackRiders`, 채팅 `action` 카드 + `supersedes`/`undone`), 호스트(공격자 권한 = 자기 캐릭터/GM, 같은 페이지 토큰 거리 → 사거리 판정, 결과를 PC 런타임(HP·임시·로그·집중 종료·0 HP 상태)이나 NPC 토큰 바(+사망 마커)/시트에 적용, 카드마다 복원 클로저를 보관해 팔레트 수정은 복원 → 같은 주사위로 재판정 → 새 카드(이전 카드 숨김), 되돌리기는 복원 + "되돌림" 카드, D90 `dmConfirmsResults`면 플레이어 판정은 "DM 확인 대기"로 두고 "적용"에서 반영), 화면: 액션 줄·시트의 "⚔" → 과녁 모드에 사거리 밝기(`range-in/long/out`) → 라이더 대화(암습·강타 슬롯) → 판정 카드(`ActionCard`: 주사위 전부(D93), vs AC·엄폐, 결과·유리/불리 배지, 이유, 피해 부분마다 주사위·유형·조정, HP 전→후·임시, 집중 내성, 부여, 사망/무의식; GM 팔레트 강제 적중/빗나감/치명타/엄폐/재굴림/피해 절반·0·±N/되돌리기), 캠페인 설정 D90 체크박스. 검사: `resolve.test.ts` 3건(79 tests), 두 탭 E2E `capture-client-attack.mjs` SC-40~42(증거 59–62).

**M4 T1 장면 우선 — Theatre of the Mind (2026-09-15)**: 격자 지도는 여기까지 두고(D95) 기본 방식을 위치 없는 "장면"으로 바꿨다. `Page.layout = "scene"`(`newScene`, 격자 꺼짐), 캠페인 설정 `tableMode`(기본 "totm"; 격자로 전환 가능), `SceneBoard`/`SceneIcon`(무대 배치: 위 NPC 카드 줄, 가운데 장면 카드(이름·배경 그림·라운드/턴 칩), 아래 플레이어 카드 줄; 카드 = 초상(해골/사람 글리프 또는 아트)·AC 방패 배지(NPC는 스탯 블록, PC는 파생 시트)·아래에서 차오르는 HP 게이지·마커·이름·턴 테두리·선택·과녁 점선; 플레이어 화면에서는 GM 전용 NPC 항목이 안 보이므로 카드 종류는 줄로 정함; 우클릭 메뉴·액션 줄·과녁 모드는 격자와 같은 코드), 호스트는 장면에서 `distanceFeet`를 만들지 않아 사거리 판단이 전혀 없고, 플레이어의 `overrides`는 `advantage`만 남기고 버린다. 판정 전 대화 `AttackDialog`(라이더 + 유리·불리 선언, DM에게는 엄폐·"반드시 적중/치명타/빗나감")는 액션 줄이 과녁 모드에서 언마운트되는 문제 때문에 캔버스의 `AttackAskBridge`로 옮겼다(R7의 라이더 대화도 같은 버그가 있었음). 벗어남(D96): `act.provoke` → `prompt` 채팅 메시지(`ReactionPrompt`) → `PromptCard`(상대 조종자에게만 근접 공격 버튼·"안 함") → `act.attack`에 `reaction`을 실어 보내면 호스트가 프롬프트 검증(반응자·대상·근접·미답변·반응 미사용) 후 "○○ · 기회 공격" 카드를 만들고 프롬프트를 답변된 카드로 교체, 트래커 행 `reactionUsed`를 켜고 그 행의 턴이 시작되면 끈다; `act.decline`. 프로토콜 v8. 검사: `scene.test.ts` 2건(81 tests), 두 탭 E2E `capture-client-totm.mjs` SC-44~47(증거 63–66); 격자 E2E들은 캠페인 설정에서 격자로 바꾼 뒤 돌린다.

**M4 T2 턴 패널 — 2024 공식 행동 (2026-09-15)**: `client/rules/actions.ts`(행동 목록 `ACTIONS`(붙잡기·밀치기·벗어나기·질주·이탈·회피·원조·은신·영향·수색·연구·활용·준비, 한/영 이름·요약·대상·기술·자유 텍스트), `ActorStats`(PC는 파생 시트, NPC는 스탯 블록; 기술 없으면 능력 수정치), `unarmedDc` = 8+근력+숙련, `resolveAction`(내성/기술 판정에 d20 전부, 붙잡힘·넘어짐·회피·이탈·도움·은신·준비·질주 마크, 벗어나기는 운동/곡예 중 나은 것), `cannotAct`(행동불능·무의식·마비·석화)), 프로토콜 v9(`act.action`; `tracker.next`는 현재 턴 조종자도), 트래커 행 `actionUsed/bonusUsed`(+`reactionUsed`, 턴 시작에 초기화), 호스트(자기 턴 검사(GM 예외), 마크는 PC 시트(진짜 상태, D84)/토큰(턴 한정 마크)에 `mark()`, 턴 끝에 이탈·질주 제거, 턴 시작에 회피·준비 제거(도움은 준 쪽의 다음 턴 시작에, 마커 `from`), 공격하면 도움·은신 소비, `combatantOf`가 PC 토큰 마크도 상태로 봄 → `suggestAdvantage`가 회피/도움/은신을 읽음, 이탈 중 `act.provoke`는 시스템 메시지로 대체), 채팅 `act` 카드(`ActCard`: 판정 주사위·DC·성공/실패·본문·표시), 화면 `TurnPanel`(자기 턴 판정: 플레이어 `controlsToken`, DM은 어느 플레이어도 조종하지 않는 토큰; 행동 경제 칩, 공격/행동/추가 행동 묶음, 대상은 과녁 모드, 기술·DC·텍스트·밀치기 방식은 `ActDialog`(브리지), 턴 마침), 액션 줄의 공격 흐름을 `makeAttackWith`로 공용화. 검사: `turn.test.ts` 3건, E2E SC-48~49(증거 67–68).

**M4 T3 알림·승인 UI, 스크롤 없는 장면, 턴 패널 메뉴 (2026-09-15)**: `client/screens/Notify.tsx`(`toastText`: 시스템·이모트·굴림·행동·공격·프롬프트를 한 줄로; `ToastLayer`: 마운트 뒤 도착한 채팅만, 4.2초, 최대 4개, 보드 위 가운데; `PromptChoices`: 기회 공격 선택(채팅 카드와 승인 도크가 공유); `ApprovalLayer`: 답 없는 프롬프트(반응자 조종자에게)와 D90 대기 카드(DM에게 적용/빗나감으로/취소), 보드 오른쪽 아래), `client/character/activate.ts`(시트의 "사용" 흐름을 공용화: `activateFeature`(점수 프롬프트·회복/임시 HP/기록 주사위·저장 시 재적용·효과 시작 적용), `usableFeatures`(활성화 규칙 있는 특성 + 남은 횟수 + 추가 행동 여부); `SheetPlay`도 이걸 씀), `client/rules/items.ts`(`itemUse`: 치유 물약 등급 2d4+2/4d4+4/8d4+8/10d4+20, 소모품 판정), `TurnPanel` 한 줄 배치 + `Dropdown`(행동/판정/특성/아이템/추가 행동; 판정은 `pcStats`/`npcStats`로 내성·기술; 특성·아이템은 저널 `putJournal`로 런타임 저장 + `/em` 알림), 장면 CSS(`overflow:hidden`, `height:100%`, 줄 최대 34vh, 무대 최소 120px), 트래커 창 기본 위치 아래(y 430). 검사: `items.test.ts` 2건(86 tests), E2E SC-50~51(증거 65, 67 갱신).

**M4 T4 상황별 초점 (2026-09-15)**: D100에 따라 장면 화면을 다시 짬. `TurnRibbon`(보드 위 절대 배치, 현재부터 순서대로, 지금/다음 라벨, DM에게 ▶ 다음 턴·트래커(`onOpenTracker`로 DM만 트래커 창); `TableScreen`은 트래커 창을 DM에게만 자동으로 염), `SceneIcon`에 `turn`(확대·금테·"행동 중" 칩)·`dimmed`(전투 중 나머지)·`candidate`(대상 지정 시 후보만 맥동, 아니면 어둡게·클릭 불가)·`floats`(`useCardFloats`: 새 action/act 메시지를 대상·행위자 카드에 매핑해 2.6초 떠오름: −피해/치명타/빗나감/자동 실패/쓰러짐/판정 성공·실패/부여 상태), `CommandBar`(보드 아래; `mode: turn|free`; turn이면 금색 "당신의 턴"(DM은 "○○의 턴")·행동 경제 칩·빨간 `.cl-btn.attack`·행동/추가 행동·시트(판정·특성·아이템·이니셔티브·시트 열기)·큰 턴 마침; free면 회색 상태 문구, 전투 중 플레이어는 판정만 활성; 대상은 `commandToken` = 내 턴 토큰 → 선택한 내 토큰 → 내 유일한 토큰), `requestTargets`에 `exclude`(자기 자신은 후보에서 제외), 대상 배너를 가운데 크게, `ApprovalLayer`를 가운데 오버레이(보드 흐림, 하나씩; 상대가 고르는 중이면 `.cl-waiting-note`), `promptIsMine`(훅 없이 판단), 토스트는 장면에서 보드가 보여 주는 것(action/act/prompt/"○○의 턴")을 건너뜀·최대 3개·대상 지정 중 숨김, 채팅 `ActionCard`는 결과 배지 우선 + "조정 ▾"로 팔레트 접기, 플레이어 장면엔 페이지 바·도구 열 없음, DM 페이지 도구는 ⋯ `Dropdown`. 격자 모드는 예전 액션 줄·턴 패널 유지. 검사: TotM E2E(리본·오버레이·대기 안내·커맨드 바·토스트 억제) + 격자 E2E 재실행.

**M4 T5 BG3식 턴 순서 바 (2026-09-15)**: `TurnRibbon`을 D101대로 다시 그림 — 현재부터 회전한 순서, 각 항목은 `.cl-turn-ribbon-item.{pc|npc}[.now][.acted][.down][.linked]`: 정사각 초상(아트 또는 해골/사람 글리프), 테두리 색(적 빨강/일행 파랑), 아래 HP 바(토큰 바 1), 임시 HP 고리(`link === "temp"` 바, conic-gradient 마스크), 쓰러짐 ✖·회색, 이미 행동한 항목 옅게; 지금 차례는 62px 금색 장식 프레임 + 이름; 연속한 일행은 그룹(BG3 연결 이니셔티브)으로 괄호를 두르고 △/▲/⌛ 화살표; 왼쪽 라운드 숫자, 오른쪽 DM 도구. 토스트는 보드 아래로 옮겨 바·대상 배너와 겹치지 않음. 검사: TotM E2E(선택자 유지) + 증거 67·68.

**M4 T6 창 크기에 맞춤 (2026-09-15)**: 테이블 화면이 창을 채움 — `.cl-page.cl-table`은 최대 폭 없이 `100vh − 52px`(상단 바) 높이의 플렉스 열, 격자 `.cl-table-grid`가 남은 높이를 차지, 사이드바 폭 `clamp(320px, 24vw, 440px)`, 캔버스는 `flex:1`(고정 `calc(100vh − 150px)` 제거), 장면 카드 폭 `clamp(88px, 7vw, 140px)`·초상 `aspect-ratio 24/31`·글리프 크기 vw 기준, 카드 줄 최대 38vh. 커맨드 바의 드롭다운(행동·추가 행동·판정·특성·아이템)은 `up`으로 위로 열림 — 아래로 열리면 캔버스의 `overflow:hidden`에 잘려 고를 수 없던 문제.

**M4 R8 주문 (2026-09-15)**: D102. `client/compendium/spells.ts`가 `spellExecutionCatalog.generated.json`을 읽어 `spellExec(id)`(대상·주 효과·집중·조건)를 준다. `client/rules/spellcast.ts`의 `resolveSpell({caster, casterStats, spec, targets, dice, overrides, apply})`가 주문 공격(R7 `resolveAttack` 재사용: 유리·엄폐·반드시·저항·임시 HP·0 HP·집중 판정), 내성 피해(범위는 주사위 한 번, 성공 없음/절반), 복합 피해, 내성 효과(상태 → 시트 상태명 `CONDITION_KO`, 지속), 회복(수정치, 최대 HP 이내), 임시 HP, 자동 명중 투사체, 여러 번 주문 공격, 추적 효과(집중)를 `SpellResolution`(대상마다 `SpellTargetResult`)로 만든다. `castableSpells(derived)`·`pcSpell(entry, derived, catalog, id, method)`가 PC의 시전 가능 목록과 슬롯 소비(`castSpell`)를 준다. 호스트 `act.cast`(프로토콜 v10): 시전자 검증(자기 캐릭터·행동 불가·슬롯) → `prepareSpell`(PC는 `pcSpell`, NPC는 스탯 블록 주문 목록: DC = 블록 DC, 명중 = DC−8, 레벨은 CR로) → 대상 수 검사 → 해결 → 시전자 시트 소비·집중 시작(NPC는 "집중" 마커) → 대상마다 `applySpellRow`(HP·임시 HP·상태·지속 효과·쓰러짐, 되돌리기 클로저) → "spell" 메시지. `act.undo`/`act.confirm`은 주문도 같이. UI: 커맨드 바 "✨ 마법 ▾"(`spellItems`), `castIt`(자기 대상은 바로, 아니면 아이콘 다중 선택 → `castOptions`로 시전 방식 대화 `CastAskBridge` → DM은 주문 공격에 판정 전 조정 대화), 채팅 `SpellCard`(줄마다 명중/내성, 그 아래 주사위·피해·HP·부여·집중·쓰러짐), 떠오름·토스트·승인 오버레이의 "spell" 처리. 커맨드 바의 인물은 이제 "내가 고른 내 인물 → 내 턴의 인물 → (플레이어) 유일한 내 인물" 순서라 DM이 남의 턴에 다른 NPC로 시전할 수 있다. 시험: `tests/client/spell.test.ts` 6개(호스트 왕복 포함), E2E SC-53(증거 69). 남은 것은 SESSION_SCENARIOS의 "R8 남은 것".

**M4 R9 전투 마무리 (2026-09-15)**: D103·D104. `client/rules/attackSpec.ts`의 `npcSaveExec(entry, actionName)`이 스탯 블록의 내성 행동을 `SpellExec`(save-damage / save-compound-damage / save-effect, 실패 상태는 effects)로 바꾸고, 호스트 `runNpcSave`가 주문 해결기로 굴려 `postSpell`(R8의 카드·D90 대기·되돌리기 경로를 메서드로 뽑음)에 넘긴다(`SpellResolution.source = "action"`, 재충전은 `runtime.spent`에 소비·복구). 프로토콜 v11: `act.npcSave`, `act.legendary`(풀 검사 → 글 행동은 `act` 카드 kind "legendary", 내성 행동은 runNpcSave, `legendaryUsed` 증가), `act.attack`/`act.cast`의 `readied`(준비 마크·반응 검사 → 이름에 "· 준비한 행동", 반응 소비, 마크 제거). 호스트는 이제 플레이어의 남의 턴 공격·시전을 기회 공격·준비·반응 주문 말고는 거절한다. 커맨드 바: `readiedNow`가 자유 모드의 공격·마법을 다시 켜고 상태 문구를 바꿈, `routine`(다중공격 루틴 → `makeAttackWith`에 `targets`·`overrides` 옵션으로 반복), `saveActions` 버튼(☄, 재충전 대기), `legendaryItems` 드롭다운, 추가 공격 ×N 배지; `castIt`은 격자에서 `from`(사거리)을 넘김; `GridFloats`가 격자 토큰 위에도 결과를 띄움; 시트의 ⚔(`JournalPanel.attackFromSheet`)도 `requestAttackOptions` 대화를 쓴다. CSS: 1100px 아래에서 커맨드 바 두 줄. 시험: `tests/client/r9.test.ts` 4개, E2E SC-54·55(증거 70).

**M4 R10 규칙 마무리 (2026-09-15)**: D105. `spellcast.ts`: `REPEAT_SAVE`/`repeatsSaveAtTurnEnd(exec)`가 실행 정의의 요약 글에서 "턴이 끝날 때 … 내성 굴림을 반복"을 찾아 `SpellEffectStart.endSave {ability, dc}`를 붙이고, `save()`는 민첩 내성에 회피면 두 번 굴려 `advantage: "회피"`·`dropped`를 남긴다. `ActiveEffect.endSave {ability, dc, conditions}`(PC), `NpcRuntime.endSaves[]`·`uses{}`(NPC). 호스트: `applySpellRow`가 endSave를 시트 효과/NPC 런타임에 적고(되돌리기 포함), `rollEndSaves(actor)`가 `nextTurn`의 턴 종료 처리에서 굴려 성공 시 `endEffect`+상태 제거 또는 endSaves/조건/토큰 마크 제거, 결과는 rollresult 카드. `prepareSpell`(NPC)이 per-day 목록의 `uses`를 세어 다 쓰면 null, `npcSpend()`로 소비·복구. 새 명령 `act.item {actor, target?, instanceId}`(프로토콜은 v11 유지): 옵션 `pcItem(entry, instanceId)`(campaigns.tsx가 `itemUse`+`setItemQuantity`로 제공) → 회복 주사위 → 대상 PC/NPC(토큰 바 포함) HP → 가방 −1 → act 카드 kind "item". UI: 커맨드 바의 아이템 메뉴가 회복 아이템이면 대상 지정 후 `c.useItem`, 판정 메뉴는 도움 마크가 있으면 `rollCheck`(두 번 굴려 높은 쪽, 마크 제거), NPC 마법 메뉴 힌트 "n/N 남음 (일)"과 비활성, `NpcSheet`에 주문 횟수 필과 "횟수 초기화", `SpellCard`에 "(회피 유리, n 버림)". 시험: `tests/client/r10.test.ts` 4개.

**M4 R11 반응·순서·확인 (2026-09-15)**: D106. `ReactionPrompt.kind`에 "shield"(`attack {name, total, ac}`, `outcome.shielded/card`). 프로토콜 v12: `act.cast.reaction`(프롬프트 id), `tracker.swap {turnId}`. 호스트: `runAttack`이 적중이고 대상이 캐릭터이며 `pcReactionSpell(entry, shield)`(campaigns.tsx: `castableSpells` + `cheapestCast`)가 시전 방식을 주고 반응이 남았으면 `held` 맵에 붙잡고 프롬프트를 말한다; `act.cast`의 reaction 검증(프롬프트 열림·반응자·반응 주문·반응 미사용) → 주문 적용 → 프롬프트 shielded 갱신 → `releaseHeld(id, true)`가 최신 전투원(시트 효과로 AC +5)으로 같은 주사위(`fixed`)와 이유 "방패 반응: AC +5"로 다시 판정해 `applyResolution`; `act.decline`은 `releaseHeld(id, false)`. `tracker.swap`은 지금 차례부터 대상까지 모두 일행·비커스텀일 때만 두 행을 바꾸고 행동 경제를 초기화. 리본 `TurnRibbon`의 `canSwap`(묶음 안 뒤 구성원, DM/조종자) → ↔ 버튼. Notify/TableScreen의 프롬프트 UI가 kind별로 갈라짐(`PromptChoices`의 방패 버튼은 `cheapestCast`). 고친 버그 둘: `sayWithId`/클라이언트 "chat" 이벤트가 같은 id를 대체하도록(D90 적용 뒤 오버레이가 남던 문제), `applySpellRow`가 캐릭터의 최신 엔트리를 쓰도록(자기 대상 주문의 슬롯 소비가 덮어써지던 문제). `resolveAttack`은 `overrides.note`를 이유에 싣는다. 시험: `tests/client/r11.test.ts` 3개, `scripts/capture-client-d90.mjs`(증거 71).

**M4 R12 통달·전설 저항·강조 (2026-09-15)**: D107·D108. `DerivedAttack.masteryKey`(derive), `pcAttackSpec`이 활성 통달을 `AttackSpec.mastery/abilityMod/masteryDc`로 싣고 `riders.cleave`면 수정치 없는 피해·"· 쪼개기" 이름; `resolveAttack`이 `MasteryResult`(graze 피해는 빗나감에도 applyDamage, topple은 `fixed.masteryD20` 지원 건강 내성, vex/sap/slow 마크, push/nick 기록)와 `attackRef/attackerRef`를 결과에 싣는다. `Combatant.vexedBy`(토큰 마커 from), `suggestAdvantage`의 약화/교란. 호스트 `applyResolution`: 스침도 피해 적용, 통달 마크는 `mark(target, …, from = 공격자 토큰)`, 기존 교란은 갱신되지 않으면 소비, 공격자의 약화는 공격 뒤 소비, `nextTurn`이 사용자 기준 도움·교란·약화·둔화를 정리. 전설 저항: `spells` 맵이 행별 되돌리기·시전 컨텍스트를 보관, `act.resist`(DM)가 그 행을 되돌리고 `resolveSpell({forceSaveSuccess, fixedDamage})`로 다시 적용해 같은 id로 카드를 갱신, `NpcRuntime.legendaryResistanceUsed`. UI: `ActionCard` ⚒ 통달 줄과 쪼개기 버튼(`requestTargets` → `c.attack(..., {cleave})`), `SpellCard` 전설 저항 버튼·배지, `NpcSheet` 카운터, 커맨드 바 ⚒ 힌트, `setHighlight`/`useHighlight`로 카드 호버 → `.cl-scene-card.highlight`, `flashCardsFor(name)`으로 리본 클릭 → 카드 `.cl-flash`. 시험: `tests/client/r12.test.ts` 5개.

**M4 R13 장면 다듬기 (2026-09-15)**: `Page.description`(페이지 설정의 textarea, `.cl-scene-desc`로 무대 아래에), 장면의 토큰 메뉴 라벨(◀ ▶, z 순서가 줄 순서), `CampaignSettings.toastSeconds/toastCount`(`ToastLayer`가 스냅샷 설정을 읽음, 캠페인 화면의 숫자 입력), `ApprovalLayer`의 키보드(`approvalKeys`: Enter → primary, Esc → 마지막 버튼), `multiattack`이 다중 선택을 차례로 배분. 문서의 남은 것 목록에서 T1·T2·T4·R7 항목이 비었고, 두루마리·NPC 특성 횟수는 데이터가 없어 보류로 남김.

**M4 R14 격자 삭제 (2026-09-15)**: D109. `client/campaign/page.ts`가 장면 전용으로 줄었다 — `Page`는 이름·순서·배경·설명·보관·토큰만, `Token`은 이름·레이어(모두/GM)·그림·대표 캐릭터·조종자·바 3개·마커·GM 노트·잠금·순서(z)만 남았고 `newPage`/`isScene`/`snap`/`clampToPage`/`columnLabel`/`cellDistance`/`TokenAura`가 사라졌다. `PageCanvas.tsx`는 1313줄에서 약 1000줄로 줄면서 격자 보드·`cl-canvas-scaler`·줌·드래그·화살표 이동·레이어 툴바·자/안개/그리기 자리·`TokenView`·`GridFloats`·`TurnPanel`·`ActionBar`를 잃었고 장면 보드 하나만 남았다(토큰 설정 창도 탭 하나로). 규칙 쪽은 `AttackSpec.rangeFeet/longRangeFeet`, `Combatant.distanceFeet`, `AttackResolution.distanceFeet`, 긴 사거리 불리, 최대 사거리 밖 자동 빗나감이 사라졌고 `weaponRange`는 근접/원거리만 답한다. 프로토콜 v13: `ping` 명령과 이벤트 삭제. `CampaignSettings.tableMode`도 삭제. CSS에서 격자 관련 39개 규칙 정리. 시험: 단위 109개 통과(`pages.test.ts`를 장면 기준으로 다시 씀), E2E는 `capture-client-{attack,pages,tracker}.mjs`를 장면으로 옮겨 12개 전부 통과(m1 스크립트의 옛 레벨 업 버튼 이름도 고침).

**마지막 점검 (2026-09-15, 소유자 지시 "부족한 게 있는지 확인")**: 코드 리뷰·규칙 커버리지 감사·브라우저 E2E 세 갈래로 점검해 고쳤다. 코드: 안수가 자신을 회복하지 않던 버그, 주사위가 구르는 동안 바꾼 HP를 굴림 결과 저장이 덮어쓰던 경쟁(저장은 `saveCharacter(source, updater)`로 저장소의 최신 런타임에 적용하고 한 번에 하나씩 실행), 지원 종료·긴 휴식 뒤 22/17이 남던 문제(저장 시 현재 HP를 유효 최대치로 클램프, `maxSeen`은 효과 없는 기본 시트 기준), 마법 갑주가 앞선 AC 보너스를 지우던 순서 의존, 저항 중복, 1시간 효과가 짧은 휴식에 안 끝나던 것. 규칙: 격노 10분(100라운드), 야생 변신 임시 HP = 레벨, 서브클래스 특성 id(`dnd.srd521.feature.<class>.<subclass>.<key>`)와 종족 특성(`species.<key>`)을 규칙 키로 정규화해 생명 보존·신성한 무기·대지의 도움·자연의 성역·떨리는 손바닥·우월한 방어 등이 실제 풀을 쓰고, 메타매직이 마법 점수를 쓰며, 무료 시전 풀(팔라딘의 강타·군마·후원자 접촉·신비한 비전·종족/재주 주문)은 `freeCastSpellId`로 주문 행에서 바로 시전된다. 브레스 무기·전술적 사고·공격 빗나가게 하기·온전한 신체는 굴림을 기록하고, 아드레날린 분출·경이로운 신진대사는 임시 HP/회복을 적용한다. 거짓 생명(임시 HP 2d4+4)·신성한 강타(슬롯별 d8)·작열/빛나는 강타는 시전 즉시 굴린다(`castHook`). 2024 규칙 정정: 저항 주문(−1d4 피해), 돌 피부, 도약, 안수 추가 행동, 자연의 장막. 파생 시트에 보호의 오라(내성 +매력), 로빙(속도 +10, 등반·수영), 용의 회복력(HP +소서러 레벨)이 추가됐다. 바드의 영감은 5레벨부터 짧은 휴식 회복. E2E: 마법 입문자 소마법이 직업 선택에 중복되던 문제(이미 아는 소마법은 비활성), 복사 피드백, 슬라이더 한 번에 한 줄 기록, 특성 굴림도 기록, 옵션 상세가 목록을 밀지 않게, 무기 속성 한글. 남은 것(수동 적용): 야수 형태 능력치, 상태 이상 자동, 주문 대상 효과, 인내의 방어/질풍 연타 같은 기 점수 세부 옵션의 타이머, 서명 주문·마법의 원천 회복 같은 풀 없는 횟수 특성.

엔진의 선택 id 규약(원본 `choices` 맵의 키): `origin.languages`, `origin.species.<choice>`(`origin.species.lineage`…), `origin.background.abilityMode|abilityPlus2|abilityPlus1|tool`, `class.<트랙>.skills|expertise|fighting-style|subclass|asi|epic-boon|...`, 직업 전체 풀은 첫 트랙에 붙는다(`class.<첫 트랙>.weapon-mastery|invocations|metamagic|cantrips|spells|spellbook`), 재주 하위 선택은 `feat.<부여 위치>.<재주 id>.<항목>`, 장비는 `equipment.class|background[.<옵션>.<n>]`. 답은 항상 옵션 id 배열이고, 옵션 밖의 답은 무시된다(모듈 제거·레벨 되돌림에도 안전).

M1에서 단순화한 것: 반복 가능한 기원술(고통스러운 폭발 등)은 한 번만 고른다. 준비 주문은 최대치보다 적어도 막지 않는다(규칙대로). ASI 후보에서 기원 재주를 뺀다(소유자 결정; 규칙상은 허용).

## 5. 결정(가정)과 열린 질문

CHARACTER_SYSTEM §10의 추천안을 M1의 가정으로 쓴다: HP 고정값 기본(D44), 능력치 직접 입력 허용·표시(D45), 시작 장비 옵션 기본(D46), 멀티클래스 허용(D47), 마일스톤이면 XP 숨김(D51), SRD 설명을 저장소에 추가(D52). 옛 저장본 이관(D43)은 M2 이후로 미룬다(새 클라이언트는 빈 라이브러리에서 시작하고 옛 캐릭터는 JSON 가져오기로 옮긴다).

열린 것: 비공개 번역 저장소(`Kaetaeru/D-D-2024-`)는 이 세션에서 접근할 수 없다. 설명 데이터는 위 4번대로 SRD 요약으로 채우고, 소유자가 나중에 생성기로 교체한다.
