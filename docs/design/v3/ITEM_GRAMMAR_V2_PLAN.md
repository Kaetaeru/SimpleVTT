# 마법 아이템 문법 2판 계획 (V0.9 IG)

소유자 지시(2026-09-23): DM마다 캠페인에서 마법 아이템을 직접 만든다. 그게 잘 돌도록 엔진 문법을 넓히고 정리한다.
제안한 묶음(문법 단계 → 캠페인 단계)을 **자율 진행**하고, 결정이 필요한 것은 방향대로 구현해 두고 끝에 모아 묻는다.
컨텍스트가 초기화되면 **이 문서부터 다시 읽는다.** 앞선 계획은 `MAGIC_ITEMS_PLAN.md`(MI-1~7, D350~D357).

## 원칙

- 붙여넣은 아이템과 모듈(공식) 아이템은 **같은 JSON**이다. 하나가 되는 것은 다른 것도 된다.
- 코드에는 아이템 이름·id가 없다(CLAUDE.md §2). 새 규칙 모양은 범용 필드·연산으로 한 번 추가하고 콘텐츠가 쓴다.
- 옛 JSON(`charges`, `resource:<항목 id>`, 두루마리 id)은 계속 읽힌다. 새 형식은 `"format": "simplevtt.magic-item/2"`.
- 슬라이스마다 코드+테스트+문서(D 번호, ROLL20_TABLE_SPEC 항목과 로드맵 줄), 커밋·푸시.

## 순서와 진행

| 단계 | D | 내용 | 상태 |
|---|---|---|---|
| IG-1 | D358 | 붙여넣기 아이템에도 `contract`. 아이템 계약은 사본마다(`resource:self` = 그 사본의 풀). 형식 버전. `schemas/magic-item.schema.json` | ✔ |
| IG-2 | D359 | 사용 횟수 여러 개: `uses[{id,label,max,recharge}]`(dawn/short-rest/long-rest/주사위/never), 주문·버튼의 `pool` | ✔ (D358에 합침) |
| IG-3 | D359 | 조건 `when`: 대상 크리처 유형, 쥐고 있음·착용·조율, 효과 발동 중 — 추가 피해·보너스에 | ✔ (착용 상태는 `worksWhen: held`만) |
| IG-4 | D360 | 조율 자격(`attunement.requires`), 저주(`curse`: 조율 해제 불가, 불이익, DM 해제 버튼) | |
| IG-5 | D361 | 주문 두루마리를 문법으로: `spellChoice`(지급할 때 주문 고르기), `authored.scroll.` 특수 경로 제거 | |
| IG-6 | D362 | SRD·PHB의 DM 판정 줄 중 새 문법으로 닫히는 것 옮기기, 하드코딩 상한 낮추기 | |
| IG-7 | D363 | 캠페인 아이템 라이브러리: DM이 캠페인에 정의 저장, 지급은 참조(고치면 모두 반영) | |
| IG-8 | D364 | 식별 안 된 아이템(`identified`), DM 식별 버튼 | |
| IG-9 | D365 | 커스텀 특성·재주 붙여넣기(아이템 계약과 같은 경로) | |
| IG-10 | — | 붙여넣기 창 미리보기(자동/버튼/DM 판정 3칸), 가이드 예시, 최종 보고 | |

나중(이번 범위 밖): NPC 하루 횟수·재충전과 `uses` 모델 통합.

## 결정 대기 (끝나면 소유자에게 묻는다)

- IG-1/2: 아이템 계약을 사본마다 등록하면서, 이미 돌고 있던 공식 아이템 효과의 키(`feature:<항목 id>#…`)가 사본 키로 바뀌었다. 업데이트 전에 켜 둔 아이템 효과는 "규칙 없음"으로 보일 수 있다 — 끄고 다시 켜면 된다. 이전 도구를 만들지 않았다.
