# V1-13 canonical validation — 2026-08-23

- result: **AUTOMATED WEB + RUST + TAURI BUILD PASS / WINDOWS INTERACTION ACCEPTANCE PENDING**
- repository: `Kaetaeru/SimpleVTT`
- canonical branch: `work/v1-composite`
- validation branch: `codex/v1-validation`
- target_sha: `4f092e08a385b29d2b08709daa67a3f7b1894a6c`
- validated_head_before_report: `5d22201287144902f87025b21df93245570b5493`
- environment: Windows PowerShell, bundled Node `v24.19.0`; repository-local Rust `1.98.0 stable-msvc`; system `npm` unavailable

## Automated checks

PASS:

- canonical remote target confirmed after user-side `git fetch origin work/v1-composite`
- Ready lifecycle focused suite: 27 baseline checks passed; the projection file then passed 3/3 with the added reconnect clear replay case
- V1-13 focused DM Library/Rations/Party Stash checks: 96/96 passed
- generated content: spells 339, progression 12 classes / 240 rows, spell rules 339, feats 17, weapons 38, class skills 12/18, built-in catalog 496
- UI named-rule boundary: scanner passed with 6 frozen findings and tests passed 3/3
- TypeScript: `tsc --noEmit` passed
- Character creation structure: 110/110 passed
- rules domain: 329/329 passed
- Campaign/Long Rest: 90/90 passed
- projection refresh follow-up regressions: 14/14 passed
- Vite production build passed (470 modules); only the non-fatal large-chunk warning remains
- `node --check scripts/live-dev-sync.mjs` passed
- acceptance launcher structure check passed in the Ready focused suite
- generated content and build left no tracked generated diff
- `cargo test --manifest-path src-tauri/Cargo.toml --lib`: 23/23 passed
- Tauri release compilation passed and produced `src-tauri/target/release/simplevtt.exe`
- exact release binary launched as two simultaneous isolated instances: Acceptance Host PID 33884 and Acceptance Client PID 1088

Environment-blocked:

- `npm ci`: no system `npm` executable. Existing validation dependency junction resolved and the pinned lockfile-backed dependency tree executed successfully with bundled Node.
- Windows two-instance interaction acceptance: both native windows launched, but the current automation surface cannot click or inspect controls inside native Tauri windows.

Tauri was compiled with the configured frontend pre-build hook disabled because that hook calls the unavailable system `npm`; the already validated production `dist` from this exact head was used instead.

## Failures found and repaired

- `4c40149` — added missing reconnect/catch-up Ready clear replay coverage.
- `492299a` — updated stale DM Library preset quick-add copy expectation.
- `d73392c` — returned the canonical persisted Campaign mutation instead of a pre-serialization object.
- `b4c9608` — isolated Party Stash approval retry transaction identities; prevented a compensated first attempt from crediting the owner without a matching retry debit.
- `6f86566` — repaired Campaign Stash policy selector syntax.
- `3c84d6a` — narrowed inventory journal grant command handling before reading quantity.
- `c537d5d` — corrected the Combat VFX bridge import.
- `286e97f` — made the Long Rest ration fixture include the active roster consumer required by the product model.
- `5d22201` — made Character projection source comparison canonical and independent of object key insertion order across persistence round trips.

## Remaining manual gate

In the two running windows, verify Host/Client Ready coexistence, single-actor trigger isolation, turn/initiative expiry, reconnect catch-up, Long Rest owner materialization, Party Stash deposit/withdrawal/approval, DM Library remote grant, Calendar advance, and Ration consumption.

## Assessment

The exact canonical base plus the validation fixes is source-complete and passes automated web, runtime, Rust, and Tauri compilation gates. Release remains **ACCEPTANCE PENDING** only for interaction checks inside the two running native Windows instances.
