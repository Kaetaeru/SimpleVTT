# Current — SimpleVTT

Updated: 2026-09-14 Asia/Seoul

## 현재 목표

한국어 D&D, 거리 계산 없는 ToTM, 빠른 전투, 화려한 비차단 연출을 중심으로 제품을 전면 개편한다.
세션뿐 아니라 개인 시트·가방·캐릭터 생성·레벨업·DM 준비·저장·연결이 같은 사용자 여정을 이루어야 한다.
소유자는 2026-09-09 대화에서 범위와 여섯 가지 사용 결정을 확정했다.

**읽을 문서:** [전면 개편 설계와 결정 기록](design/v2/PRODUCT_RENOVATION.md).
실행 순서는 같은 문서 §10, 수락 시나리오는 §11, 소유자 결정은 §12다.
**할 수 있어야 하는 것 전체:** [기능 인벤토리](design/v2/CAPABILITY_INVENTORY.md) — 모든 행동의 규칙 근거·비용·실행 등급(자동/질문/DM 판정/서술).
소유자 지시(2026-09-11): 구현 전에 기획을 확실히 한다. 인벤토리는 소유자 결정 D7~D11을 반영해 확정됐다(§20). 다음은 P0을 이어 가는 것이다.

## 2026-09-14 새 클라이언트 — 전면 재작성 (소유자 지시)

소유자 지시: "모든 부분을 전면적으로 새로 만든다는 느낌으로. 멀티세션은 나중으로 미루고, 캐릭터 시트를 완벽하게 생성 가능하게. JSON도."
계획은 [새 클라이언트](design/v3/NEW_CLIENT.md), 캐릭터 모델은 [캐릭터 시스템](design/v2/CHARACTER_SYSTEM.md).

- **M1 캐릭터 생성 — 코드 완료(C1·C2·C3), 소유자 exe 확인 대기.** `client/`(옛 `src/`를 import하지 않음, `npm run check:client-boundary`).
  카탈로그(SRD 모듈 36 + 색인 + 진행표 + 주문 339 + 저작 SRD 요약 설명) → `deriveCharacter` 엔진(원장·선택 요청·검증) → IndexedDB 저장 → JSON v2 내보내기/가져오기 → RuleModule JSON 설치 → 화면(라이브러리·마법사·시트·콘텐츠).
- 검사: `npm run test:client` 40건(전수 행렬: 종족 9 × 직업 12 × 배경 4 × L1·L5 + 직업 12 × L1~20, 설치 모듈 흐름, JSON 왕복, 저장소, 화면 렌더). 증거 캡처 15장 `docs/evidence/new-client-m1/`.
- exe: 워크플로 `windows-exe-client.yml` → 프리릴리스 `client-m1-*` (`SimpleVTT-client-<sha>.exe`). 옛 V2 미리보기 exe 워크플로는 그대로 있다.
- 소유자가 보고한 세 문제(레벨업에 기원 재주, 종족 특성 미표시, 드워프 HP)는 새 엔진에서 검사 항목으로 고정됐다(`derive.test.ts`, `matrix.test.ts`, `install.test.ts`).
- 다음: M2 시트 운용(사용량·가방·휴식·레벨업 UI 다듬기) → M3 테이블 이식 → M4 멀티세션.

## 현재 실제 상태

- 기존 제품 통합: `work/v1-composite` @ `cfd7279279ed58acb29f7c29832ee7c136e6b25e` (조사 시점).
- 새 통합 대상: 기존 `work/v2-table`. 조사 시점에는 같은 SHA이며 V2 기능은 아직 통합 전이다.
- 설계 PR #389와 오프라인 커널 PR #390은 조사 시점 **open / unmerged**.
- 이 설계 브랜치는 #390 head `198bacf5e84326e44666d7ea905fb136e9819db5`를 기반으로 한다.
- 이번 변경은 설계/라우팅뿐이다. P0~P6 구현이나 새 Windows 앱 수락은 완료되지 않았다.
- #390의 이전 CI 성공은 해당 커널의 증거이며, 전체 개편의 완료 증거로 재사용하지 않는다.

## 2026-09-11 진행 (브랜치 `claude/practical-newton-rye61w`, #390 위에 stacked)

- V2 커널이 기능 인벤토리 대부분을 오프라인 테스트로 덮는다: 자세·손·바닥·투척·즉흥 무기·아군 대상, 붙잡기/밀치기/탈출,
  질문 큐(기회공격·준비 발동·비치명), 즉흥 행동→DM 판정 카드·서술·요청·즉석 규칙, 주문(도메인 커널)·물약·휴식·안정화·숨기·열린 판정,
  Host/Client 연결(수신자별 필터·재접속·명령 중복 제거·소유자 write-back 훅). 상태표는 [인벤토리 §0.1](design/v2/CAPABILITY_INVENTORY.md#01-구현-상태--2026-09-11-브랜치-claudepractical-newton-rye61w-v2-커널-srctable).
- (당시) 옛 화면은 facade로 새 런타임을 렌더링했다 (`?table=v2`). 2026-09-14 T2-07 전반 이후 facade가 기본이고 옛 화면은 `?table=legacy`다.
- 생성·시트·레벨업은 기존 화면·기존 adapter가 facade 프록시를 통해 그대로 동작한다. 새 시트 모델(P1/P2)은 미착수.

## 2026-09-14 공식 규칙 대조 감사 (SRD 5.2.1 / 2024)

- 소유자 지시 "지금까지의 진행이 D&D 공식 세션 플레이에 맞는 방향인지 확인하고 부족한 부분을 기획에 포함"에 따라 기획과 커널을 규칙 영역 19개로 대조했다. 결과는 [인벤토리 §22](design/v2/CAPABILITY_INVENTORY.md#22-공식-규칙-대조--srd-521-2024-기준-2026-09-14).
- **판정: 방향은 맞고 깊이가 모자란다.** 19개 영역 모두 부분 일치, 이탈 없음. 남은 항목 336건(필수 19 · 높음 150 · 낮음 167, 그중 나중 41). 구조적 구멍 다섯 가지: 시계가 흐르지 않음, 시야·엄폐·공포가 상수, 결과 확정 전 반응 창 없음, 직업·종족·특기 기능 미노출, 몬스터 스탯 블록 절반 무시.
- 기존 행 정정 30건(2014 규칙이 남은 행: 긴 휴식 히트 다이스 절반, 붙잡힌 채 끌기 이동 절반, 물약 먹이기 행동 등)과 소유자 결정 D12~D41이 같은 절에 있다.
- 소유자 결정 D42(2026-09-14): 엄폐·시야·닿음 같은 상황 판정은 DM이 한다. 공격이 들어오면 DM이 적중/빗나감/치명타 강제, 엄폐, 유리/불리, 피해 수정을 고를 수 있다(기본은 사후 토글, 선택 설정으로 사전 개입). 다섯 구멍 중 "사실이 상수" 항목은 사실 파생이 아니라 이 팔레트로 푼다.
- 소유자가 2026-09-14 추천안을 승인한 결정 16건(D12·D19·D21·D22·D24~D28·D30·D34·D37~D41)은 인벤토리 §20에 있다. 교전은 이동·사망·장면 전환에만 끝나고(D25), 이탈은 면제 표시(D27), 세션 중 휴식은 DM 제안(D30), 성장은 마일스톤 기본(D34).
- 다섯 구멍의 구현 명세는 [규칙 런타임 명세](design/v2/RULES_RUNTIME_SPECS.md)에 있다: 시계 → 반응 창과 DM 개입 팔레트(D42) → 직업 기능 원장과 자원 모델 → 장면·액터 종류·저장. 다음 구현은 이 순서다. 감사 원본(발견·검증 판정)은 저장소 밖 작업 폴더에만 있다.

## 2026-09-14 진행 — 규칙 런타임 명세 네 슬라이스 구현 (같은 브랜치)

- 소유자 지시 "전부 단일 에이전트로 V1 완료를 목표로 실행"에 따라 [규칙 런타임 명세](design/v2/RULES_RUNTIME_SPECS.md) §1~§4를 순서대로 커널에 넣었다: 시계 → 반응 창·D42 팔레트 → 직업 기능 원장 → 장면·물체·소환·저장·write-back·D22 비공개. 요약과 남은 목록은 [인벤토리 §0.2](design/v2/CAPABILITY_INVENTORY.md), 게이트별은 [V2 로드맵](roadmap/V2_TABLE_REWRITE.md).
- 이어서 슬라이스 5(`dc1436e0`)와 6(`fd557bf4`, `72fa7dd8`)으로 남은 커널 항목을 넣었다: 몬스터 재충전·전설 행동·다중공격 루틴·명중 부가 효과, 공포·투명·숨음 공격 수정, 표식·강타 류 부가 효과, 불굴·바드의 영감 재굴림 창, 무기 숙련 6종, 야생 변신.
- 증거: `npm run test:table` 61건, `tsc`, 경계 검사 둘, `vite build` 모두 green (Linux 오프라인). 화면(T2-04)·Windows 실기기·전환(T2-07)은 여전히 하지 않았다.
- V1 완료 기준으로 남은 커널 항목(작음): write-back의 나머지 내구 항목 가운데 탈진·조율(시트 모델 필드 필요). 슬롯·히트 다이스·죽음 내성·안정·사망은 기존 시트 필드(`resources`·`hitDiceByDie`·`durableLifeFlags`)로 쓴다.
- 소유자 지시 "바로 가보자"로 화면(T2-04)에 들어갔다. 슬라이스 A: [DM 워크스페이스 설계](design/v2/DM_WORKSPACE.md)의 세 영역·한 문법을 `src/table/ui/`에 구현 — 상단 바, 테이블(토큰 카드·초점 카드·D42 판정 메뉴·질문), 사이드바 7탭(기록·전투·액터·아이템·자료·규칙·세션), 명령 센터(테이블에서 대상 지정), DM 재량 바 / 플레이어 자유 행동 바, 토큰 HUD, Ctrl+K, 시트 서랍. `?table=v2`일 때 dev 미리보기와 라이브 세션이 이 화면을 쓴다. 증거는 [T2-04.md](roadmap/evidence/T2-04.md). 슬라이스 B: 호스트 라이브러리(`src/table/library.ts`: 내 NPC·PC 프리셋·내 아이템·장면 묶음, 캠페인 없이 브라우저 저장소에 유지)와 캠페인 화면의 `준비실`, 세션 액터·아이템 탭의 같은 목록, 묶음 소환, 드래그 소환, 아이템 지급(`grant-item`: 무기는 공격 타일로)과 토큰 드롭. 소유자 피드백으로 시각 체계를 한 번 다시 잡았다(13px·32px 컨트롤·두 단계 표면). 슬라이스 C: 자료 탭이 호스트 소유 이미지·노트(공개는 모두 또는 토큰 드롭으로 한 플레이어에게만, `handout`), 기록에서 결과 카드 재열람. 내 NPC는 초상·설명·DM 메모·능력치를 가진다. 남은 것: 소유자 검토, Windows.
- 전환(T2-07) 전반: `main.tsx`가 테이블 facade를 기본으로 구성한다(플래그 없이 캠페인 → 세션 시작 → 세션 열기 → DM 워크스페이스). 옛 화면은 `?table=legacy` 또는 `localStorage.simplevtt.table="legacy"`로만 열린다. 시나리오 3 "DM 재량"(자연 1 재량·즉흥 행동·손·다수 대상 상태·되돌리기)은 `tests/table/scenario3.test.ts`로 오프라인 green. 옛 세션 adapter는 아직 구성돼 있다(생성·시트·캠페인 시스템이 그 위에서 돌고, 삭제는 `tests/ui` 약 150건과 Windows 러너를 함께 지우는 일이라 소유자 결정). 증거 [T2-07.md](roadmap/evidence/T2-07.md).
- 커널 잔여분: 무기 숙련 베어가르기(무거운 무기 명중 뒤 다른 생물에게 한 번 더, 능력 수정치 없음, 턴마다 한 번)와 찌르기(가벼운 무기 추가 공격을 공격 행동의 일부로, 추가 행동 유지)를 타일로 넣었고, 야생 변신 중에는 다음 턴부터 형태의 이동 속도가 적용되며 카드가 이동·감각을 적는다 (`tests/table/mastery.test.ts` 8건, `features.test.ts`). 몬스터 주문 시전: 스탯 블록의 주문 가운데 공격·내성 주문은 이미 타일이었고, 나머지(투명화·비행·마법 탐지 같은 유틸리티·강화)도 이제 설명이 붙은 텍스트 타일이 되어 목록의 하루 사용 횟수를 풀로 쓴다 (`statblock.test.ts` 마법사).
- write-back이 제품 경로에서 실제로 저장된다: 이전에는 facade의 `persistTableSheet` 훅이 구현돼 있지 않아 세션의 HP·임시 HP·자원이 캐릭터 라이브러리에 닿지 않았다. 이제 `mutateCharacterDurably`(id 기준)로 솔로/호스트의 로컬 캐릭터는 커밋마다, 클라이언트는 Host 이벤트마다 한 번씩(실제 변화만) 라이브러리 레코드와 활성 시트에 쓴다. 실패는 `facade.lastWriteBackError`에 남는다. 내구 항목은 HP·임시 HP·자원·주문 슬롯·히트 다이스·죽음 내성·안정·사망이며, 다음 테이블은 그 시트로 캐릭터를 다시 앉힌다(쓴 히트 다이스는 쓴 채로) (`tests/table/facade.test.ts` 솔로·연결 write-back 2건).

## 2026-09-14 소유자 결정 — 캐릭터 시스템 재설계

- 실기기(exe) 검사에서 생성·성장·시트의 결함 세 건이 나왔다: 레벨업 재주 목록에 기원 재주가 나옴(내장·설치 모두), 시트에 종족 특성 미표시, 드워프 강인함이 레벨업에 미적용. 원인은 모두 "파생값 계산이 화면별로 흩어져 있고 원본이 아닌 문자열을 저장"하는 구조다.
- 소유자 지시 "검사를 할 바엔 제대로 기획해서 다시 만들자"에 따라 [캐릭터 시스템 재설계](design/v2/CHARACTER_SYSTEM.md)(P1+P2)를 기획했다: 원본(`CharacterSource`)·계산 하나(`deriveCharacter`)·사용량(`CharacterRuntime`), 내장·설치 통합 카탈로그, 저장 v2와 이관, 슬라이스 C1~C6, 전수 검사. 소유자 결정 D43~D52를 기다린다. 세 건의 낱개 수정본은 `git stash`에 보관하고 적용하지 않았다.

## 다음 작업: P0

1. #389/#390과 이 설계의 실제 live GitHub 상태를 다시 확인하고 통합 순서를 정리한다.
2. [P0 작업 패킷](design/v2/P0_WORK_PACKET.md)에 따라 저장본/캐릭터/세션 경계의 좁은 실험을 만든다.
3. 특히 빗나간 근접 공격의 교전, 수신자별 비공개 데이터, 명령 중복과 소유자 저장 확인을 검증한다.
4. 그다음 P1 개인 시트/가방 → P2 생성/성장 → P3 짧은 실전 순서로 진행한다.

T2-02의 주문 목록만 확장하거나 과거 W7-05 NEXT로 돌아가지 않는다.
소유자의 후속 지시가 이 파일보다 우선하며, 진도가 바뀌면 이 페이지를 갱신한다.

## 과거 V1 증거

V1 완료 선언은 [W9-04](roadmap/evidence/W9-04.md),
[V1 ledger](roadmap/V1_EVIDENCE_LEDGER.json), [V1 master roadmap](roadmap/V1_MASTER_ROADMAP.md)에 보존한다.
기존 72/72 등의 수치를 새 제품의 완료율로 표시하지 않는다.
