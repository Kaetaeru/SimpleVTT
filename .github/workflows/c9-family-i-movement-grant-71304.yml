name: C9 Family I Movement Grant 71304

on:
  push:
    branches:
      - agent/c9-gate-n-coverage-reconciliation
    paths:
      - tests/ui/c9FamilyIMovementGrantProduction.test.ts
      - rules/profiles/dnd.srd-5.2.1.profile.json
      - .github/workflows/c9-family-i-movement-grant-71304.yml
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run generate:content
      - run: node --import tsx --test tests/ui/c9FamilyIMovementGrantProduction.test.ts tests/ui/c9FamilyIMovementProduction.test.ts
      - run: node scripts/check-v1-mechanism-coverage.mjs
      - run: node scripts/check-legacy-execution-boundary.mjs
      - run: npm exec tsc -- --noEmit
