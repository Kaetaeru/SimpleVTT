name: Rerun C9 Actor Lifetime Diagnostic

on:
  push:
    branches:
      - "agent/c9-gate-n-coverage-reconciliation"
    paths:
      - ".github/workflows/rerun-c9-actor-lifetime-diagnostic.yml"
      - "tests/ui/connectedActorArtifactLifetimeProduction.test.ts"
      - "src/domain/resolutionArtifactOps.ts"
      - "src/domain/resolutionTurnOps.ts"
      - "src/domain/runtimeArtifact.ts"
      - "src/domain/commonPlayArtifactRuntime.ts"

permissions:
  contents: read

jobs:
  focused:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npm run generate:content
      - run: npx tsx --test tests/ui/connectedActorArtifactLifetimeProduction.test.ts
