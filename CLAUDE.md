# SimpleVTT — 작업 기준 (반드시 지킬 것)

**컨텍스트가 압축되었거나 새 세션이면, 작업을 이어가기 전에 이 파일을 처음부터 다시 읽는다.**
**슬라이스를 시작할 때와 커밋하기 전에 §3 점검표를 실제로 돌린다.** 요약이나 기억으로 대신하지 않는다.

진행 방식(단일 에이전트, 슬라이스 = 코드+테스트+문서, 한국어 보고, 커밋 형식 등)은 `docs/HANDOFF.md`에 있다.

---

## 1. 플레이 가능의 기준

소유자가 정한 목표: **플레이어는 시전하고, 공격하고, 고르기만 한다. 계산할 수 있는 것은 전부 앱이 한다.**

1. **계산 가능한 것은 전부 자동.** 명중·피해·내성 굴림, 보정치와 주사위(축복 d4 등), 유리·불리, 저항·면역·흡수,
   상태 부여와 해제, 지속시간과 그 기준 턴, 집중과 집중 판정, 자원·슬롯 소비, 턴당 1회 같은 사용 제한,
   턴 시작·끝 효과, 처치·휴식·이니셔티브 같은 순간의 특성, 자동 결과의 되돌리기.
2. **앱이 볼 수 없는 사실은 맥락 상황 버튼으로.** 좌표가 없으므로(D109) "들어감 / 빠져나감 / N피트 이동함 /
   인접 아군 있음" 같은 버튼을 그 상황이 걸렸을 때만 띄운다. 플레이어가 수치를 손으로 넣지 않는다.
3. **열거할 수 있는 선택은 선택 창으로.** 창마다 항상 / 안 함 / 묻기 정책을 두고, 묻는 창은 턴 단위로 묶는다.
4. **열거할 수 없거나 서사적인 것은 "DM 판정" 라벨.** 카드나 시트가 끝난 것처럼 보이면 안 된다.
   손으로 처리할 것이 남으면 그 자리에 "DM 판정" 또는 "수동 처리 필요"라고 적는다.
5. **DM의 결정은 정책 설정으로.** DM 확인은 플레이어가 선언한 카드에만 건다(D223).
6. **SRD만이 아니라 설치한 모듈 콘텐츠도 똑같이 동작해야 플레이 가능이다.** SRD 이름으로만 돌아가는 자동화는
   완료로 치지 않는다 → §2.
7. **"된다"의 증거는 테스트다.** 새 동작마다 호스트 경로(카드가 실제로 나오고 HP·상태가 바뀌는지)를 검사하는
   테스트를 둔다. 단위 함수만 확인하고 완료라고 하지 않는다.

## 2. 하드코딩의 기준

모듈은 JSON뿐이라 코드를 넣을 수 없다. 코드에 박힌 규칙은 모듈 콘텐츠에서 작동하지 않는다.

**코드에 두는 것 (엔진)**
- 범용 문법: 계약 연산의 종류, 트리거 시점, 창·구역·반응의 메커니즘, 몬스터 특성 패턴 실행기, 식 평가기.
- 5e 핵심 규칙 어휘: 상태의 효과(장님·넘어짐·마비 …), 피해 유형, 능력치·기술, 슬롯표·경험치표, 소마법 5/11/17.
- UI 표기, 통신·보안 한도, ID 체계 해석.

**JSON에 두는 것 (콘텐츠)**
- 특정 직업·서브클래스·특성·재주·기원술·주문·몬스터·아이템의 **이름, ID, 수치, 주사위, DC, 레벨표, 조건, 선택지**.
- 어떤 콘텐츠가 어떤 범용 문법을 쓰는지.

**금지 (예외 없이)**
- 코드에서 특정 콘텐츠의 ID·이름·slug·특성 키를 비교하거나 상수로 두는 것
  (`"dnd.srd521.spell.hunter-s-mark"`, `key === "body-and-mind"`, `slug === "paladin"` 등).
- 특성 이름이나 설명문을 정규식으로 읽어서 규칙을 추측하는 것 (`/Undead Fortitude/`, `/턴이 끝날 때/`).
- 범용 문법의 이름을 특정 콘텐츠 이름으로 짓는 것 (`hunters-mark.die` ✗ → `marked-spell.die` + `spellId` 매개변수 ○).

**새 규칙 모양이 필요할 때**
1. 기존 문법으로 표현되는지 먼저 확인한다.
2. 안 되면 범용 연산을 코드에 **한 번** 추가한다(이름과 매개변수는 콘텐츠 중립).
3. 그 연산을 쓰는 콘텐츠는 JSON(계약·SRD 색인·모듈)에 적는다.
4. 스키마(`schemas/*.json`)와 모듈 작성 가이드도 같이 갱신한다.

**예외**: JSON으로 만드는 것이 불가능하거나 오히려 손해일 때만. 그 경우 코드에 `hardcode:` 주석으로 이유를 적고
`docs/design/v3/HARDCODE_AUDIT.md` §4에 기록한다. "빨리 하려고"는 이유가 아니다.

전수 목록과 옮기는 순서: `docs/design/v3/HARDCODE_AUDIT.md`.

## 3. 점검표 (슬라이스 시작 전, 커밋 전)

- [ ] 이 슬라이스가 §1의 어느 항목을 채우는가? 남는 수동 처리는 "DM 판정"으로 표시했는가?
- [ ] 새로 쓴 코드에 콘텐츠 ID·이름·slug·특성 키 비교, 이름·설명문 정규식이 있는가? → 있으면 §2대로 JSON으로.
- [ ] 새 문법 이름이 콘텐츠 중립인가? 스키마·가이드를 갱신했는가?
- [ ] 같은 규칙이 코드와 JSON 두 곳에 있지 않은가?
- [ ] SRD가 아닌 모듈 콘텐츠로 같은 동작을 재현하는 테스트가 있는가?
- [ ] 호스트 경로 테스트가 있는가? 게이트(`npm run gate`)를 통과했는가?
- [ ] 커밋 전 확인: `git diff --cached -U0 -- client | grep -E '"dnd\.srd521\.|key === "|slug === "|nameEn'` 결과가 늘지 않았는가?
