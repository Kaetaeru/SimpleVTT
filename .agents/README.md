# Agent Instructions

This directory is reserved for user/operator instructions that guide coding agents working on this repository.

## Boundary

- Keep agent-specific operating instructions under `.agents/`.
- Do not place SimpleVTT product requirements, source code, runtime configuration, or general project documentation here.
- Keep GitHub-specific configuration and automation under `.github/`.

This separation keeps agent context distinct from the product itself and reduces repository noise as the project grows.
